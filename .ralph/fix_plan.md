# Fix Plan — Website Agency Pipeline

The pipeline is built in 12 iterations (0–11). Each iteration must end with a working demo. Don't jump ahead. Read `.ralph/specs/09-iterations.md` for full acceptance criteria per iteration.

The implementation repo is created from the GitHub template `reaandrew/cloud-skeleton` and customized via `bin/init.sh`. **Iteration 0 is the bring-up of that skeleton plus our agency-specific extensions.**

Marking convention: `[ ]` not done, `[~]` in progress, `[x]` done.

---

## High Priority — Iteration 0.A: Bring up cloud-skeleton

- [x] **0.A.1** Create implementation repo from template `reaandrew/cloud-skeleton`. Clone locally. *(done 2026-05-08: `reaandrew/ai-website-agency`, cloned to `/home/parallels/Development/ai-website-agency`.)*
- [x] **0.A.2** Run `bin/init.sh --dry-run` to preview, then run for real with the project flags. Commit. Delete `bin/init.sh`. *(done 2026-05-08: `--project ai-website-agency --account-id 276447169330 --base-domain agency.techar.ch --parent-domain techar.ch --github-org reaandrew --github-repo ai-website-agency --aws-vault-profile personal_iphone`. Renamed `lambda-edge/finance-*.js` and `cloudfront-functions/finance-cookie-to-auth.js` since `init.sh` substitutes contents but not filenames. `bin/init.sh` deleted.)*
- [x] **0.A.3** Copy `.ralph/`, `.claude/`, `.github/pull_request_template.md`, and `CLAUDE.md` from the spec repo into the implementation repo. Commit. *(done 2026-05-08.)*

> **RALPH: STOP HERE.** Items 0.A.4–0.A.6 are a one-time human ceremony — they require iPhone-MFA-gated `aws-vault exec` and editing NS records on the parent zone. Do **not** attempt `aws-vault`, `terraform apply`, or DNS edits autonomously. When you reach this point in the plan, emit `STATUS: BLOCKED` with `RECOMMENDATION` pointing the operator at `docs/BOOTSTRAP.md` and stop the loop. After the operator completes 0.A.4–0.A.6 and ticks the boxes, you'll resume cleanly at 0.A.7.

- [x] **0.A.4** **HUMAN ONLY.** AWS bootstrap per `docs/BOOTSTRAP.md`: state bucket, first apply, NS records, second apply, SSM contract, GH secrets. Run with `aws-vault exec personal_iphone -- terraform …` from `aws-setup/`. *(done 2026-05-08: confirmed by green Deploy workflow on push-to-main + `https://agency.techar.ch/` returning 200.)*
- [x] **0.A.5** **HUMAN ONLY.** First production deploy via `git push origin main`; verify all three URLs respond. *(done 2026-05-09: Deploy run 25595407615 ✅ on `main` after #5 merge; admin `https://agency.techar.ch/` → 200; API GW + Lambda exist (`/healthz` route is iter-0.E work). Worker is on `*.workers.dev` for now — `previews.agency.techar.ch` route was intentionally dropped in commit `f5f8dff`.)*
- [x] **0.A.6** **HUMAN ONLY.** Verify per-PR env: open + close a no-op PR; cleanup must succeed. *(done 2026-05-08/09: PRs #1–#5 each opened-and-merged with green Deploy + Cloudflare workflows on the `pull_request` trigger.)*
- [x] **0.A.7** Create stub `CLAUDE.md` files in `lambdas/`, `terraform/`, `worker/`, `frontend/` per `11-agents.md` § "Sub-directory CLAUDE.md files". Each is short and references the spec. *(done 2026-05-08; `worker/` directory created as a placeholder for iter 0.D.2.)*

## High Priority — Iteration 0.B: CI Efficiency

Apply the changes in `.ralph/specs/12-ci-efficiency.md` to `.github/workflows/deploy.yml` (and `security.yml`) before adding any agency-specific resources to `terraform/`. Doing this first means every subsequent iteration's PR pipeline is fast.

- [x] **0.B.1** Add `pre-flight` job to `deploy.yml` emitting `frontend_changed`, `lambdas_changed`, `terraform_changed`, `cloudflare_changed`, `worker_changed`, `code_changed`, `run` (via `dorny/paths-filter@v3`).
- [x] **0.B.2** Gate `deploy`, `smoke-tests`, `post-deploy-seed`, `e2e-tests`, `dast-zap`, `accessibility-lighthouse`, `load-test-k6` on `code_changed == 'true'` (doc-only PR fast lane).
- [x] **0.B.3** Path-filter the heavy jobs per the table in `12-ci-efficiency.md` § Win 2.
- [x] **0.B.4** Move `sca` to `security.yml` (weekly cron + on `**/go.mod`/`**/package*.json` changes + manual). Keep `iac-scan` in `deploy.yml` but gated on `terraform_changed`. Also dropped `aquasecurity/tfsec-action@v1.0.3` (deprecated upstream + GitHub-API rate-limit on its anonymous binary download); Trivy in `security.yml` covers IaC scanning.
- [x] **0.B.5** Merge `lint-go` + `test-go-unit` into one `go-quality` job. Merge `lint-frontend` + `test-frontend-unit` into one `frontend-quality` job.
- [x] **0.B.6** Cache Terraform providers via `TF_PLUGIN_CACHE_DIR` + `actions/cache@v4` keyed on `**/.terraform.lock.hcl` in every job that runs `terraform init` (`provision-branch`, `lint-terraform`, `deploy`).
- [x] **0.B.7** Drop `closed` from `deploy.yml`'s `pull_request: types`. Remove now-redundant `action != 'closed'` clauses from per-job `if:`.
- [x] **0.B.8** Shallow checkout (`fetch-depth: 1`) for `secret-scan` on PRs (uses `base`/`head` SHAs).
- [x] **0.B.9** Centralize `if:` logic into the `pre-flight` job's `outputs.run` and have downstream jobs reference `needs.pre-flight.outputs.run == 'true'`.
- [x] **0.B.10** Add top-level `Makefile` `ci-local` target replicating the gating jobs locally.
- [ ] **0.B.11** Verify all acceptance criteria in `12-ci-efficiency.md` § "Acceptance for iteration 0.B as a whole" by opening test PRs (doc-only, frontend-only, lambda-only, terraform-only, mixed) and recording wall-clock vs the unmodified skeleton baseline.
- [x] **0.B.12** Branch fast lane: broaden `deploy.yml` `push` trigger to all branches; gate the heavy chain (deploy / smoke / post-deploy-seed / e2e / dast-zap / lighthouse / k6) on `pull_request` events or `push` to `main` only. Push to a feature branch runs only quality + build (lint, test, scan, compile) — no per-PR ephemeral env until a PR is opened. Drops the obsolete `provision-branch` job (covered by `push: '**'`).

## High Priority — Iteration 0.C: Agency-specific Terraform

- [x] **0.C.1** Extend `terraform/dynamodb-items.tf` with `gsi1`, `gsi2`, `gsi3` per `02-data-model.md`.
- [x] **0.C.2** `terraform/eventbridge.tf`: custom bus `pipeline${local.env_suffix}` + archive + scheduler group + hourly discovery rule (disabled) + weekly tuner rule (disabled). *(also adds the three weekly tuner rules at 02:00 UTC Sunday + the scheduler invoke role.)*
- [x] **0.C.3** `terraform/cognito.tf`: user pool, hosted UI, app client, JWT authorizer on the API. Seed one operator with out-of-band password. *(seeding the first operator is a manual `aws cognito-idp admin-create-user` step — not in code; documented in `docs/BOOTSTRAP.md` once iter 0.C lands.)*
- [x] **0.C.4** `terraform/ses.tf`: configuration set, suppression list, bounce/complaint SNS topic. Sender `outreach.<base>`. *(includes domain identity + DKIM CNAMEs + MAIL FROM domain; SES sandbox-out is a separate manual step before iter 8 sends real email.)*
- [x] **0.C.5** `terraform/bedrock-iam.tf`: ONE grouped `bedrock-invoke` policy on the two model ARNs (skeleton pitfall #6).
- [x] **0.C.6** `terraform/s3-blobs.tf`: `pipeline-blobs${local.env_suffix}` bucket; `force_destroy=true` non-prod.
- [x] **0.C.7** `terraform/sqs.tf`: one DLQ per planned consumer; 14-day retention. *(14 consumers seeded — discover, audit, qualifier, backlog-promoter, spec-generator, generator, publisher, email-draft, sender, reply-triage, ses-feedback, tuner-targeting, tuner-style, tuner-email-tone.)*
- [x] **0.C.8** `terraform/kms.tf`: KMS CMK `passcode-cleartext-${env}` for KMS-encrypting passcode cleartext (used by publisher + email-draft). Auto-rotation enabled.

## High Priority — Iteration 0.D: Cloudflare R2 + Worker (passcode-gated)

- [x] **0.D.1** New `cloudflare/` Terraform stack: R2 bucket `previews${local.env_suffix}` (90d lifecycle on `keep!=true`); Workers KV namespace `PREVIEW_PASSCODES_${env}`; Workers Rate Limiting binding; route `previews.<base>/*` → Worker. *(rate-limit implemented as a `cloudflare_ruleset` rather than a Workers Rate Limiting binding — Cloudflare's zone-level ruleset is cleaner per the spec's "10 req / 60s per IP on POST" intent and the Worker doesn't need to call the binding from code.)*
- [x] **0.D.2** New `worker/` (TypeScript): R2 binding, KV binding, Rate Limit binding, `PASSCODE_SALT` secret. Routes:
  - `GET /sites/{websiteId}` and asset paths under it: cookie or `?p=<passcode>` validation; passcode form on miss. ✅
  - `POST /sites/{websiteId}` (form submit): passcode form handler. ✅
  - `GET /screenshots/{websiteId}/{size}.png`: same passcode rule. ✅
  - `GET /healthz` returns 200. ✅
  - **HMAC-SHA256 signed cookie scoped to `/sites/<websiteId>/` ✅. Constant-time argon2id compare**: scaffolded with **SHA-256-with-salt** in this iter (still constant-time-compared); argon2id WASM swap is iter 5.4 work (publisher in 5.3 and Worker in 5.4 will land the swap together so they don't drift on hash format).
- [x] **0.D.3** `.github/workflows/cloudflare.yml`: deploy `worker/` and apply `cloudflare/` Terraform on changes. Secrets: `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`. *(`worker-quality` job (typecheck + lint + format:check + test); `deploy` job runs Terraform apply then `wrangler deploy` with the per-env script name.)*

> **0.D follow-ups (not blocking 0.D merge)**:
> - `destroy.yml` doesn't tear down the cloudflare/ stack on PR close → per-PR R2 buckets + KV namespaces accumulate. Add a `cloudflare-destroy` step or sibling workflow before iter 5.3 (when real preview content lands).
> - `worker/src/passcode.ts` swap from SHA-256-with-salt to argon2id (WASM). Coordinated with iter 5.3 publisher Lambda (the hash format must match across both writers).

## High Priority — Iteration 0.E: `lambdas/pkg/` extensions

- [x] **0.E.1** `pkg/events/`: envelope, publisher, consumer wrapper. *(done 2026-05-09: `Envelope[T]` generic with eventId auto-gen + `WithCorrelation`/`WithCausation` chainers + `Validate`; `Publisher` with injectable `EventBridgeAPI` and `EVENT_BUS_NAME` env-var bootstrap; `Unmarshal` / `FromEventBridge` / `FromSQS` decoders; `Consume[T]` partial-batch-failure SQS dispatcher. 89.1% coverage.)*
- [x] **0.E.2** `pkg/idempotency/`: `WithIdempotency[T]` per `stdlib/idempotency-patterns.md`. *(done 2026-05-09: `WithIdempotency[T any](ctx, consumer, eventID, fn)` with `IDEMP#<consumer>#<eventID>` PK per the spec's "second pattern" footnote, conditional `attribute_not_exists(pk)` write, `ErrAlreadyProcessed` for replays, 24h TTL via `expires_at`. Required `pkg/ddb` extension: lazy `Client(ctx)` + `SetClient` test hook + `TableName()` reading `ITEMS_TABLE`. Coverage: idempotency 92.0%, ddb 94.7%.)*
- [x] **0.E.3** `pkg/cost/`: `Record`, `Get`, `Assert`, `WithCostCap`. *(done 2026-05-09: daily-spend ledger at `pk=CAP#<YYYY-MM-DD>, sk=STAGE#<stage>` per `05-capacity-and-cost.md`; atomic `UpdateItem ADD spentUsd, callCount`; 30d `expires_at` TTL; `Assert` returns `ErrBudgetCapExceeded` when `spent + estimate > cap`; `WithCostCap[T]` orchestrates Assert→fn→Record with fn returning `(T, actualUsd, err)` so token-billed calls record true cost. Cap is an explicit param — `pkg/killswitch` (0.E.9) will source it from PipelineSettings. Widened `pkg/ddb.API` with `GetItem`/`UpdateItem`. 93.1% coverage.)*
- [x] **0.E.4** `pkg/bedrock/`: `InvokeStructured[T]` per `stdlib/json-output-conventions.md`. *(done 2026-05-09: full pipeline — cache hit → `cost.Assert` → `InvokeModel` with forced `tool_choice` + temp 0.2 default → tool_use extraction (`ErrNoToolUse` if absent) → injectable `Validator` → unmarshal into `T` → `cost.Record(actualUsd)` from token usage → cache-write with 30d TTL. Pricing table in `pricing.go` for Haiku 4.5 + Sonnet 4.6 (per-token, not per-million); `SetPricing` for runtime overrides. `Validator` interface with `nopValidator` default — `pkg/schemas` (0.E.6) wires the real one at startup. 90.2% coverage.)*
- [x] **0.E.5** `pkg/politefetch/`: robots-aware HTTP client. *(done 2026-05-09: `Client.Fetch` does robots.txt allow-check (24h DDB cache + RFC 9309 5xx → disallow / 4xx → allow); fleet-wide per-host token bucket via DynamoDB conditional `UpdateItem` (default 1 req per 5s, honours `Crawl-delay` if stricter); ETag/Last-Modified cache (24h TTL) short-circuiting on 304; 429/5xx exponential backoff with decorrelated jitter (3 retries default); documented bot UA `WebsiteAgencyBot/1.0 (+…/bot)`. Uses temoto/robotstxt for parsing. 85.2% coverage.)*
- [x] **0.E.6** `pkg/schemas/`: JSON-Schema generation from Go structs. *(done 2026-05-09: `JSONSchemaFor[T]() (json.RawMessage, error)` reflects via invopop/jsonschema with `DoNotReference`+`RequiredFromJSONSchemaTags`; strips `$schema`/`$id` so the result is the bare object shape Bedrock tool-use expects. `Validator` validates payloads with santhosh-tekuri/jsonschema/v5, caches compiled schemas; structurally satisfies `bedrock.Validator` so lambdas wire it via `bedrock.SetValidator(schemas.NewValidator())` at startup. 94.6% coverage; integration_test.go pins the structural-typing contract.)*
- [x] **0.E.7** `pkg/prompts/`: versioned prompt templates from `07-bedrock-prompts.md`. *(done 2026-05-09: framework only — `Prompt[T]` struct, `New[T]` constructor (panics on missing fields, eagerly populates Schema via schemas.MustJSONSchemaFor[T]), `Apply` to convert into bedrock.InvokeInput, `HashInputs` deterministic input-hasher with unit-separator (avoids segmentation collisions), `WrapBlock` XML-block helper, `SafetyRulesBlock` constant referencing 10-quality-rules.md. Each prompt's actual file (audit_qualitative_v1.go, spec_v1.go, etc.) lands alongside the consumer Lambda iteration that uses it — that's also when bedrock-prompt-reviewer fires. 100% coverage.)*
- [x] **0.E.8** `pkg/passcode/`: 8-char Crockford-Base32 generator, argon2id hasher, KMS encrypt/decrypt helpers, KV writer (Cloudflare API). *(done 2026-05-09: `Generate()` 8-char Crockford-Base32 (alphabet excludes I/L/O/U) via crypto/rand.Int — no modulo bias; `Hash(passcode, salt)` uses SHA-256 of `<passcode>|<salt>` matching `worker/src/passcode.ts:hashPasscode` (cross-check vector pinned in tests so the format stays in lockstep — argon2id swap is iter 5.4 work, coordinated with publisher 5.3); `ConstantTimeEqual` via `crypto/subtle`; `IsValidPasscodeFormat` for early-rejection in publisher; `EncryptCleartext`/`DecryptCleartext` via KMS SDK with base64-wrapped ciphertext for DDB string storage; `KVWriter` posts hash via Cloudflare bulk-PUT (`/storage/kv/namespaces/<id>/bulk`) with metadata, `Delete` for revocation. argon2id deferred — pulled `golang.org/x/crypto v0.51` would have forced go directive to 1.25 (CI is 1.24.13). 92.6% coverage.)*
- [x] **0.E.9** `pkg/killswitch/`: `Allowed(ctx, stage)` checks `pipelineEnabled` + per-stage flag from `PipelineSettings` (cached 60s). *(done 2026-05-09: `Get(ctx)` reads singleton `pk=SETTINGS#PIPELINE, sk=CURRENT` with `ConsistentRead=true` (operator-toggle latency); 60s in-process cache; `Allowed(ctx, stage)` returns `pipelineEnabled && stages.<stage>Enabled`; `CapUSD(ctx, stage)` maps stage → `Budgets.*` for `pkg/cost.WithCostCap`; `StageMap` documents the consumer-Lambda → operator-stage rollup (publisher → preview, qualifier → audit, etc.); `Defaults()` mirrors `05-capacity-and-cost.md`. `SetSettings`/`SetCacheTTL` test hooks. Missing-row error explicitly references the 0.F.1 seed step. 92.3% coverage.)*

## High Priority — Iteration 0.F: Pipeline controls

- [x] **0.F.1** Seed `PipelineSettings` singleton via `aws_dynamodb_table_item` with `lifecycle.ignore_changes=[item]`. Defaults per `05-capacity-and-cost.md`. *(done 2026-05-09: `terraform/pipeline-settings.tf` creates `pk=SETTINGS#PIPELINE, sk=CURRENT` once with the documented defaults; `lifecycle.ignore_changes=[item]` mirrors the SSM-placeholder pattern (Pitfall #4) so operator edits via /settings persist across `terraform apply`s. Drift guard: `lambdas/pkg/killswitch/seed_test.go` parses the heredoc out of the .tf file, unwraps the DDB-typed JSON, and asserts it equals `Defaults()` — a doc bump that updates one without the other now fails the test.)*
- [x] **0.F.2** `lambdas/api-settings/`: `GET /settings`, `PATCH /settings`. Operator-only via Cognito group claim. *(done 2026-05-09: new `lambdas/pkg/auth/` parses `cognito:groups` from the V2 HTTP API JWT-claims context tolerantly — handles `[operator]`, `["operator"]`, bare `operator`, comma/space variants — and exposes `IsOperator(req)`. `lambdas/api-settings/` routes on `req.RequestContext.HTTP.Method`: GET serves the killswitch-cached row (60s warm-container); PATCH reads fresh from DDB (consistent), validates that body keys ⊆ {pipelineEnabled, stages, caps, thresholds, budgets}, unmarshals each top-level key onto the populated Settings struct (so sub-objects deep-merge field-by-field — a single cap update keeps the rest), PutItem-replaces the row, then `killswitch.SetSettings(&new)` so the same warm container's next consumer-Lambda call sees the change without waiting on the 60s TTL. Both routes are JWT-protected at the API Gateway via `aws_apigatewayv2_authorizer.cognito` and additionally gated to operator-group in the handler. Reuses the `lambda_api` IAM role — DDB read/write was already granted.)*
- [x] **0.F.3** Kill switch enforcement at every (currently-empty) consumer entry. *(done 2026-05-09: `lambdas/pkg/killswitch/wrap.go` adds `WithKillSwitch(ctx, stage, fn func(ctx) error) error` — folds the three required steps into one entry call: pre-flight `Allowed(ctx, stage)`, on disabled emit a structured `pipeline.<stage>.skipped_killed` log line (msg + stage + metric fields, parseable by CloudWatch metric filters) and return nil so the Lambda runtime treats it as a successful no-op, on allowed call `fn(ctx)` and surface its error verbatim. Fails closed on `Get` errors so a transient DDB hiccup retries via DLQ rather than silently bypassing the gate. Unknown stage rejects in tests rather than shipping a fake-gated consumer. There are no consumer Lambdas yet — first usage lands in iter 1.3 (`lambdas/discover/`) and iter 2.3 (`lambdas/audit/`). 7 tests; 100% coverage on the new file.)*
- [x] **0.F.4** Cost ledger items + daily rollover Lambda at 00:05 UTC. *(done 2026-05-09: cost ledger items already shipped in iter 0.E.3 (`pkg/cost`) — this iteration adds the rollover. New `StagePauseReasons` typed struct on `Settings` mirrors `StageFlags` one-for-one with `omitempty` JSON tags; reserved `PauseReasonBudget="budget"` constant the cost-cap mechanism writes when a daily cap is hit and the rollover matches against. `lambdas/cost-rollover/main.go` reads the singleton (consistent), the pure `rollover()` decision function flips Stages.<X>Enabled back to true and clears the pause reason for any stage where Enabled=false AND reason==budget, PutItem persists, then primes the killswitch cache. Operator-disabled stages (no pause reason set) are left untouched — that's the contract that lets operators keep stages off across days. EventBridge Scheduler `cron(5 0 * * ? *)` ENABLED on every env (5-min offset gives DDB metric rollups time past midnight); reuses the existing `scheduler_invoke` IAM role whose wildcard already covers `ai-website-agency-*` Lambdas. 9 tests covering pure-function rollover (no-op, budget-paused single + multi, operator-paused untouched, unknown-reason untouched, mixed sources) + handler integration (no-op, persist, read-error, write-error, cache-prime). Note: the spec at `02-data-model.md` documents cost ledger pk as `COST#YYYY-MM-DD` while `pkg/cost` shipped using `CAP#YYYY-MM-DD`; pre-existing divergence from iter 0.E.3, out of scope here.)*

## High Priority — Iteration 0.G: Admin shell

- [x] **0.G.1** Add `react-router-dom`. Routes: `/`, `/queue`, `/settings`, `/login`. Replace `pages/Hello.tsx` with `pages/Dashboard.tsx`. *(done 2026-05-09: `react-router-dom@^7` added; `frontend/src/App.tsx` is now the routed shell — header + nav strip + `<Outlet/>`; `main.tsx` mounts `<BrowserRouter><Routes><Route path="/" element={<App/>}>` with index/`queue`/`settings`/`login` children. Four stub pages added: `Dashboard.tsx` (BFF /health surface — same content the old Hello.tsx had, ready for KPIs to replace it later), `Queue.tsx` (empty list placeholder for iter 6.1+), `Settings.tsx` (placeholder for iter 0.G.3 form), `Login.tsx` (placeholder; the actual Cognito Hosted UI redirect is iter 0.G.2 since it needs runtime-config plumbing). Hello.tsx deleted. SPA fallback already handled by both CloudFront distributions — production via `custom_error_response`, preview via the existing `preview_router` Lambda@Edge that rewrites extensionless paths to `/<env>/index.html`. Frontend tests: api.test.ts + new App.test.tsx (6 cases) green; @testing-library/react + jest-dom installed; vitest test-setup wired. typecheck clean, lint clean, build clean.)*
- [x] **0.G.2** Auth guard reading runtime-config + cookie presence; redirect to Cognito Hosted UI URL. *(done 2026-05-11: new `frontend/src/AuthGuard.tsx` reads `document.cookie` for an entry named `auth_token` (same name the substrate's cookie-to-auth CFFn looks for) and, when absent, calls `window.location.replace(COGNITO_LOGIN_URL)`. main.tsx splits the route tree: `/login` is public, everything else (Dashboard/Queue/Settings) is wrapped in `<Route element={<AuthGuard/>}>` and renders the protected `<Outlet/>` only on cookie presence. `/login` itself also redirects on mount via the same URL, so it works as the click-through "Sign in" destination. RuntimeConfig gains `cognitoHostedLoginUrl`; empty string disables the redirect for local dev so `npm run dev` doesn't bounce to an unreachable Cognito URL. `scripts/deploy-frontend.sh` builds the URL from terraform outputs (`cognito_hosted_ui_domain`, `cognito_app_client_id`) passed via env vars from the workflow's "Capture endpoints" step. Callback URL hard-mirrors `terraform/cognito.tf`'s `callback_urls` shape. E2E health spec sets the cookie via `context.addCookies` so the test still exercises the BFF /health round-trip. 4 new AuthGuard tests; 12 frontend tests total all green; typecheck/lint/format/build clean.)*
- [x] **0.G.3** Settings page reads/writes `PipelineSettings` via the BFF; master kill switch + per-stage toggles + cap sliders with cost preview. *(done 2026-05-11: `frontend/src/pages/Settings.tsx` GET-on-mount via the new `getSettings()` API client; controlled form for master toggle, four per-stage flags, six daily caps, three budget caps, three thresholds; auto-paused tag rendered next to any stage whose `stagePauseReasons.<stage>` is "budget" (only the cost-rollover Lambda flips this back). Save submits the whole edited object as a PATCH; the api-settings Lambda's deep-merge means a partial-shape submit would also work but the full-shape submit avoids ambiguity. Cost preview is a pure `computeDailyCostUsd(settings)` function exposed for testing — unit costs ported from `.ralph/specs/05-capacity-and-cost.md` § Cost model; total and per-stage figures recompute live as the operator edits caps. 8 new Settings tests + 2 cost-preview tests; 20 frontend tests total all green; typecheck/lint/prettier/build clean.)*
- [x] **0.G.4** Bring across the access-strip skeleton component (used in iter 5+) — empty state for now. *(done 2026-05-11: `frontend/src/components/AccessStrip.tsx` implements the layout documented in `.ralph/specs/08-admin-ui.md` § "Single candidate" — Access URL row with [Copy URL]; Code row with [Copy code] / [Show/Hide] / [Regenerate code]; cleartext-window countdown. Props are all optional so the empty state (no preview yet) renders cleanly. Show/Hide toggles state-local visibility; the four actions are wired to optional callbacks so iter 5.3 (publisher) + 6.3 (queue-detail page) can drop them in. Past-cleartext-window date renders "Code wiped — regenerate to view" and hides Copy code / Show. `formatRemaining()` helper does the "Nd Mh" countdown, frozen-clock tested. Queue.tsx mounts the empty-state component so the layout is visible on the per-PR env without waiting for iter 6. 8 new AccessStrip tests; 28 frontend tests total all green.)*
- [x] **0.G.5** OAuth callback handler — exchange Cognito authorization code for `auth_token` cookie. *(done 2026-05-11: not originally pinned in the spec but required for the auth flow to actually work end-to-end. `frontend/src/auth.ts` does the SPA-side PKCE dance — `beginPkceFlow(loginUrl)` generates a 384-bit random verifier, stashes it in sessionStorage, returns the login URL with the matching S256 `code_challenge`. `frontend/src/pages/Callback.tsx` registered at `/oauth/callback` (public route) reads `?code=` from URL, retrieves the verifier, POSTs to Cognito's `/oauth2/token` endpoint, gets back the id_token, writes it as the non-HttpOnly `auth_token` cookie (Max-Age 30min, Path /, SameSite Lax, Secure on HTTPS), wipes the verifier, navigates to /. Errors render a recoverable "Start again → /login" link. AuthGuard internal-navigates to `/login` (not a hard redirect) so Login.tsx's PKCE prep always runs before the external bounce. RuntimeConfig gains `cognitoAuthOrigin` + `cognitoClientId` + `cognitoRedirectUri`; deploy script emits them. Tradeoff: cookie is non-HttpOnly (XSS-readable) because the SPA sets it from JS; a future iter can move the exchange to a BFF Lambda for HttpOnly. 5 new Callback tests + restructured AuthGuard tests + Login simplified; 34 frontend tests total green.)*

## High Priority — Iteration 1: Targeting Profile + Discovery

- [x] **1.1** `TargetingProfile` model + CRUD API (`lambdas/api-targeting/`) + UI at `/settings/targeting`. *(done 2026-05-11: new shared `lambdas/pkg/targeting/` package owns the `Profile` struct (mirrors `02-data-model.md`), Validate (weights sum to 1.0 ± 0.01, required fields), and the five CRUD helpers (Create/Get/List/Update/Delete). Stats are server-managed: Create zeroes them, Update preserves the persisted values, so an API caller can't fabricate historical performance. Etag is rotated on every write; ConditionExpression on the etag turns into the 412 `ErrEtagMismatch` sentinel on optimistic-concurrency failure. `lambdas/api-targeting/` Lambda routes five paths through the standard operator-auth gate (same Cognito JWT + group-claim check as api-settings); If-Match header carries the etag on PATCH. New `terraform/api-targeting.tf` wires log group + Lambda + integration + the five JWT-authorized routes; reuses lambda_api role (DDB read/write already granted). The shared `ddb.API` interface was widened to include `Scan` + `DeleteItem` (an Explore agent stubbed the nine existing test fakes). Frontend: api.ts gains TargetingProfile types + four CRUD clients; `frontend/src/pages/Targeting.tsx` is a list view with inline editor — keyword comma-split, weight inputs with live sum validation that disables Save when the sum drifts from 1.0, server-rotated etag carried in `If-Match` on PATCH so a stale-write returns 412 → "Save failed" message. Settings page now links to `/settings/targeting`. 8 targeting tests + 12 api-targeting tests + 8 frontend Targeting tests + 4 frontend api/route updates; 42 frontend tests total; Go test suite clean; typecheck/lint/format/build all clean.)*
- [x] **1.2** Discovery providers in `lambdas/discover/providers/`: `companieshouse`, `googleplaces`, `csv`. Each returns normalized `DiscoveredBusiness`. *(done 2026-05-11 across two PRs. 1.2a: shared `lambdas/pkg/discovery/` package (`DiscoveredBusiness`, `Provider` interface, `FindRequest`) + CSV provider (operator-uploaded S3 CSV → DiscoveredBusiness; pure `parseCSV` exposed for testing). 1.2b: CompaniesHouse + GooglePlaces. CompaniesHouse uses HTTP Basic auth with the API key as username + empty password (per CH docs), free; query built from vertical + location + include-keywords, response items mapped with active=0.85 / dissolved=0.6 confidence, domain stays empty (CH carries no websites — audit Lambda resolves later). GooglePlaces uses POST /v1/places:searchText with X-Goog-Api-Key + an X-Goog-FieldMask that narrows the response to (id, displayName, websiteUri, formattedAddress) — keeps the per-call cost down to the documented $0.017. Domain is normalised through `normaliseDomain` (strips scheme + www + trailing path; rejects no-dot strings). Confidence: 0.9 with website, 0.6 without. Both providers reject 401/403 with "API key rejected", surface 429 as "rate-limited — discover Lambda should back off" so the iter 1.3 caller can implement backoff. politefetch deliberately NOT used here — these are authenticated API endpoints with their own rate limits, not scrape-style fetches that need robots.txt enforcement. cost.WithCostCap wrapping happens at the discover Lambda level in 1.3, not in the provider itself. 11 CH tests + 13 Places tests + 11 CSV tests; full Go test sweep + golangci-lint clean.)*
- [x] **1.3** `lambdas/discover/` triggered by EventBridge Scheduler hourly rule (enabled here). Idempotent on lowercased domain. Publishes `business.found`. *(done 2026-05-11: `lambdas/discover/main.go` is the first real consumer Lambda. Handler wraps `killswitch.WithKillSwitch(StageDiscovery)` so pipeline-off → no-op + skipped_killed metric. `run()` is the testable core taking a `runDeps` struct (ListProfiles, Providers, Publish, BudgetUSD, Now, NewBizID); production wires the real impls in `buildDeps()`. For each enabled TargetingProfile × each provider: `cost.WithCostCap` wraps the Places call against `Budgets.DailyPlacesUsd`; on `ErrBudgetCapExceeded` the provider is added to a `skipProvider` set so subsequent profiles don't retry it this run (cost-rollover Lambda re-enables tomorrow). Per-domain dedup via `Query` on `gsi3` (`DOMAIN#<lowercased>`) before any PutItem. `attribute_not_exists(pk)` ConditionExpression on the PutItem handles the race-window where two providers in the same run produce the same domain. Companies-House results without domains are skipped + logged (audit Lambda resolves later, iter 2). Publishes `business.found` envelope via `pkg/events.Publish`; publish failures are logged but the row stays persisted — downstream consumers can recover via the `BUSINESS#STATUS#new` gsi1. Terraform: `terraform/discover.tf` adds Lambda + log group + two SSM parameters for the API keys (placeholders + `lifecycle.ignore_changes=[value]` so operator-supplied keys aren't clobbered on apply). Schedule `aws_scheduler_schedule.discover_hourly` updated from `DISABLED` to `ENABLED` with the real ARN target. The shared `ddb.API` interface was widened with `Query` (11 fake stubs added across the test suite by an agent). 10 discover tests covering happy-path, disabled-profile skip, dedup-known-domain, no-domain skip, lowercase normalisation before dedup, cap-exceeded-skips-rest, provider-failure-isolation, publish-failure-doesn't-rollback-DDB, ListProfiles error surface. Full Go test sweep + golangci-lint + terraform fmt/validate clean.)*
- [x] **1.4** `/metrics` discoveries widget; "Run discovery now" button. *(done 2026-05-11: new `lambdas/api-metrics/` Lambda with two operator-only routes — `GET /metrics/discoveries` queries gsi1 (`BUSINESS#STATUS#new`) for the 50 most recent businesses and computes a 7-day per-day count + total; `POST /metrics/discoveries/run` synchronously invokes the discover Lambda via the AWS SDK (per-domain dedup makes manual + scheduled runs safely co-exist). New `terraform/api-metrics.tf` adds log group + Lambda + 2 routes + the narrowest possible `lambda:InvokeFunction` IAM grant scoped to the discover Lambda's ARN only — no wildcards. Frontend: api.ts gains DiscoveryRow/DiscoveriesResponse types + `getDiscoveries()` + `runDiscoveryNow()` clients; `frontend/src/pages/Metrics.tsx` renders the 7-day count with ASCII bars + recent-list table; nav strip gains "Metrics" link between Queue and Settings. Closes iter 1 (Targeting + Discovery). 11 api-metrics tests + 7 Metrics frontend tests; 49 frontend tests total; Go test suite + golangci-lint + terraform fmt/validate all clean.)*

## High Priority — Iteration 2: Audit Engine

- [x] **2.1** Cheap technical pre-audit (`lambdas/pkg/audit/technical/`): PageSpeed API, HTML heuristics. No Bedrock. *(done 2026-05-11: pure Go package with `Result{HTTPS, Viewport, Favicon, ContactDetected, Lighthouse{Performance, Accessibility, SEO}, HomepageHash}`. `Auditor.Audit(ctx, url)` fetches the homepage via the injected `PoliteFetcher` (politefetch IS used here — robots.txt + per-host throttle applies to homepage scrapes), runs `heuristics()` (HTTPS from URL scheme, viewport meta regex, favicon link regex, ContactDetected via mailto/email/UK-phone regexes; SHA-256 of body becomes `HomepageHash` for the iter 2.2 cache key), then optionally calls PageSpeed Insights v5 via the injected HTTPDoer with API key from `PAGESPEED_API_KEY` (audit Lambda iter 2.3 wires it from SSM). PSI failure degrades gracefully — Lighthouse stays zeroed and Audit still returns a usable Result. `WorthQualitativeAudit(result, minTechnicalIssueScore)` is the pure decision fn the iter 2.3 Lambda uses to gate the (expensive) Bedrock call: weights HTTPS=30, Viewport=25, Favicon=5, Contact=10, plus (60-lighthouseCategory) for any category <60 (skipped when score==0 to handle PSI-down); sum vs threshold (default 30 from `PipelineSettings.Thresholds.MinTechnicalIssueScore`). 17 tests covering heuristics edge cases (case-insensitive matchers, single/double-quoted attrs, apple-touch-icon as favicon, UK phone formats, false-positive guards), Auditor happy-path + PSI degrade + 404/empty-url errors, hash determinism, pct rounding/clamping, WorthQualitativeAudit boundaries.)*
- [x] **2.2** Bedrock qualitative audit (Haiku 4.5) gated on technical threshold; cached on `(domain, html_hash)` TTL 30d via `expires_at`. *(done 2026-05-13: three pieces. (a) `lambdas/pkg/schemas/audit_qualitative_v1.go` — `AuditV1{Score,WorthRedesigning,Summary,Issues}` + `AuditIssue{Type,Severity,Description}` structs with `jsonschema:` tags including the closed `enum=` lists for type (conversion|design|performance|mobile|trust|seo|accessibility) and severity (low|medium|high); first project use of `jsonschema:"enum=…"` syntax — verified against invopop/jsonschema reflect.go that comma-separated `enum=v` entries each append to `t.Enum`. (b) `lambdas/pkg/prompts/audit_qualitative_v1.go` — `AuditQualitativeV1` (Haiku 4.5, tool `recordAudit`, MaxTokens 800, EstimateUSD 0.012, CacheTTL 30d) with system message mirroring 07-bedrock-prompts.md verbatim + SafetyRulesBlock; PostValidate enforces non-empty Summary/Description and rejects the banned word "password" in either (belt-and-braces for SafetyRulesBlock drift). (c) `lambdas/pkg/audit/qualitative/qualitative.go` — `Run(ctx, Input, capUSD)` builds the cache key per spec (`bedrock.CacheKey(promptID, HashInputs(domain, htmlExcerpt))`) and assembles the user message in XML-ish blocks (business/technical/html_excerpt). Defensively truncates `HTMLExcerpt` to `MaxHTMLExcerptBytes = 8KB` before hashing so a caller that forgets to strip+truncate still gets a sane cache key + cost envelope. Cache TTL configured on the Prompt (not Run); `bedrock.InvokeStructured` handles the cache read/write natively. 5 + 9 + 11 tests total covering schema generation (enums, maxItems, additionalProperties:false, validator end-to-end), prompt config (ID/model/tool/maxTokens/stage/estimate/cacheTTL/PostValidate present), system-message contents, PostValidate happy path + banned-word + empty-field + describing-testimonials-passes cases, byte-stable snapshot of the assembled tool-use payload at `pkg/prompts/testdata/audit_qualitative_v1.snapshot.json` (per 07-bedrock-prompts.md § Snapshot tests; `UPDATE_SNAPSHOTS=1` to regenerate after intentional prompt changes), Run happy path + cache hit skipping Bedrock + cache key derivation + truncation behaviour + Domain-required input validation + Bedrock-error surfacing + buildUserMessage block assembly + truncateBytes unit cases. Gating logic (`WorthQualitativeAudit`) already lives in pkg/audit/technical (iter 2.1); the iter 2.3 consumer Lambda will be the place those compose.)*
- [x] **2.3** `lambdas/audit/` consumer; stores `Audit`; publishes `website.audit.completed`. DLQ + 3 retries. *(done 2026-05-13: SQS-driven consumer of `business.found`. `handle` wraps the whole batch in `killswitch.WithKillSwitch(StageAudit)` then drives `events.Consume[BusinessFoundDetail]` with partial-batch-failure semantics; per-record `processRecord` wraps the work in `idempotency.WithIdempotency("audit", env.EventID)` so SQS replays are no-ops. `runOne` looks up the Business row, runs `technical.Auditor.Audit` (politefetch homepage + PageSpeed), gates the qualitative call on `WorthQualitativeAudit(result, settings.Thresholds.MinTechnicalIssueScore)` (default 30) — below threshold → skip Bedrock and store technical-only audit with worthRedesigning=false. Above threshold → `qualitative.Run(in, capUSD)` (capUSD from `killswitch.CapUSD(StageAudit)`). Snapshots homepage HTML to S3 at `audits/<auditId>/homepage.html` (failure non-fatal — clears SnapshotS3Key, still persists the row). Persists `AuditRow` per 02-data-model.md § Audit (pk=BUSINESS#<id>, sk=AUDIT#<id>, gsi1pk=AUDIT#WORTH_REDESIGNING#<bool>). Publishes `website.audit.completed` carrying `modelsUsed=["pagespeed"]` always plus `"anthropic.claude-haiku-4-5"` when qualitative ran. Correlation + causation IDs propagated from the inbound `business.found` envelope. Production deps assemble a politefetch Client + technical.Auditor + S3 client; the `Audit` adapter avoids double-fetching the homepage by feeding the politefetch body to a `staticBody` PoliteFetcher for the second `technical.Auditor.Audit` call. Plumbed an HTML-tag stripper (`stripToText`) that drops `<script>`/`<style>` blocks then tags + collapses whitespace before the 8KB excerpt cap inside `qualitative.Run`. Tests: 11 cases covering above/below-threshold paths, missing/empty-domain Business rows, fetch failure surfacing as error (→ SQS retry → DLQ), snapshot failure non-fatal, idempotent replay using a real `ConditionalCheckFailedException` shape, killswitch off, bad-body BatchItemFailure plumbing, stripToText script/style/tag handling, buildQualitativeJSON nil-vs-populated. Terraform: `audit.tf` adds the Lambda (memory 512, timeout 120s, `provided.al2023` x86_64, `BLOBS_BUCKET` + `EVENT_BUS_NAME` + `PAGESPEED_API_KEY` env), SQS main queue (visibility 180s > Lambda timeout, retention 4d, `maxReceiveCount=3` → `dlq["audit"]`), EventBridge rule `business-found-to-audit` with retry_policy + dead_letter_config, queue policy allowing EventBridge `SendMessage` scoped to the rule ARN, event source mapping with `ReportBatchItemFailures`, one new inline IAM policy granting SQS consume on the new queue, PAGESPEED_API_KEY SSM placeholder + ignore_changes (mirrors discover.tf providers), audit log group in lambda-log-groups.tf. `/review-iteration 2.3`: cost-idempotency-reviewer PASS (kill switch right stage, idempotency per-record, cost cap chain verified to bedrock.InvokeStructured's cost.Assert, politefetch scope correct, 30d TTL written on cache miss), skeleton-pitfall-reviewer PASS (#3/#4/#5/#6 patterns aligned with discover.tf, nosemgrep marker, no math/rand, runtime + arch correct).)*

## Medium Priority — Iteration 3: Qualification + Backlog

- [x] **3.1** `priorityScore` pure function with golden tests. *(done 2026-05-14: `lambdas/pkg/qualifier/priority.go` implements the formula from 05-capacity-and-cost.md term-for-term: `auditScore*websiteNeed + verticalFit*verticalFitScore + businessSize*sizeProxy + contactConfidence*contactConf + websiteAge*websiteAgeProxy`, with a final `clamp01` so adversarial weights or out-of-range audit scores can't escape `[0,1]`. Input types `Audit{Score,LighthousePerformance,HasContact}`, `Business{Vertical,Confidence}`, `Contact{Confidence}` are minimal subsets of the row shapes — keeps the qualifier independent of `lambdas/audit`'s row marshaling. `sizeProxy` reuses `Business.Confidence` as a stand-in until iter 6.x adds real headcount signals via Companies House officer counts; `websiteAgeProxy` returns 0 when PageSpeed didn't run (LighthousePerformance=0 sentinel) rather than treating it as "very old", with a +0.1 bump when contact wasn't detected on the homepage. `verticalFitScore` is case-insensitive (manual ASCII fold via `eqFold` so vertical strings tagged `Accountants` vs `accountants` match), never returns below 0.5 — the spec deliberately avoids black-holing near-misses. 10 golden fixtures pin numbers derived by hand for: weak-site-vertical-match-high-contact (0.83), strong-site-with-contact (0.355), vertical-mismatch (0.68), no-contact-row (0.52), PSI-didn't-run (0.51), median-across-board (0.55), audit-heavy custom weights (0.8475), everything-zero-except-mismatch-vertical (0.05), out-of-range score clamps (0.78), case-insensitive match (0.55). Plus property tests: always-in-range under adversarial weights, contact-vs-nil delta = exactly `Weights.ContactConfidence * Contact.Confidence`, verticalFit floor of 0.5, sizeProxy bounded by confidence, websiteAgeProxy with PSI-down + ceiling caps. Total 18 test cases.)*
- [x] **3.2** `lambdas/qualifier/` consumer. *(done 2026-05-14: SQS-driven consumer of `website.audit.completed`. EventBridge rule pattern filters on `detail.worthRedesigning=[true]` so audits the technical gate already rejected never reach the qualifier. `handle` wraps the batch in `killswitch.WithKillSwitch(StageAudit)` (qualifier rolls up to audit stage per StageMap) and drives `events.Consume[AuditCompletedDetail]` with partial-batch-failure; `processRecord` wraps the per-record work in `idempotency.WithIdempotency("qualifier", env.EventID)`. Loads Audit + Business rows; picks the TargetingProfile by `findProfileForBusiness` (case-insensitive vertical match, newest-enabled wins; iter 6.x will swap to explicit profileId propagation through the event chain). Builds `qualifier.Input` (Contact is nil for v1 — iter 6.x enrichment), runs `PriorityScore`, compares `priorityScore*100 >= Thresholds.MinQualificationScore` (default 70). Writes the `Qualification` row per 02-data-model.md (pk=BUSINESS#<id>, sk=QUAL#<id>, qualified bool, priorityScore, reasons, auditId, targetingProfileId). Flips `Business.status` to "qualified"/"rejected" via UpdateItem with attribute_exists(pk) guard + refreshed gsi1pk; status-update failure logs but doesn't fail the run (qualification row is source of truth). Publishes `website.qualified` or `website.rejected` (rejected event carries reasons — spec lists name but not shape; mirrored the qualified shape + reasons array for the operator UI). Correlation + causation propagated from inbound envelope. `buildReasons` is a deterministic projection: high-severity audit issues count, very-slow-site, contact-hard-to-find, not-HTTPS, vertical-fit-high, high-confidence-source, well-converted-when-rejected — falls back to "score below threshold" when nothing fires. Terraform: `qualifier.tf` adds Lambda (256MB, 60s timeout, `provided.al2023` x86_64), SQS main queue (visibility 120s > Lambda timeout, retention 4d, maxReceiveCount=3 → dlq["qualifier"]), EventBridge rule with `worthRedesigning=[true]` event-pattern filter + retry_policy + dead_letter_config, queue policy scoped to the rule ARN, event source mapping with ReportBatchItemFailures, one new inline IAM policy for SQS consume on the new queue, qualifier log group in lambda-log-groups.tf. 16 test cases covering weak-site-qualifies, strong-site-rejected, borderline-threshold-flips-decision, missing-audit/business/profile short-circuits, profile-selection (case-insensitive + newest-enabled wins + disabled-skipped), idempotent replay, correlation+causation propagation, publish-failure-surfaces-error, kill switch off, bad-body BatchItemFailure, buildReasons happy/default/well-converted paths. `/review-iteration 3.2`: cost-idempotency-reviewer PASS (kill switch right stage, idempotency per-record, no paid calls / scrape fetches / cache writes to verify), skeleton-pitfall-reviewer PASS (all 20 pitfalls checked clean).)*
- [x] **3.3** Backlog logic + `backlog-promoter` Lambda triggered by `queue.slot.freed`. *(done 2026-05-14: three pieces. (a) `lambdas/pkg/queue/queue.go` — `CountActiveSlots(ctx)` runs two gsi1 Select=COUNT Queries (BUSINESS#STATUS#awaiting_review + BUSINESS#STATUS#qualified with FilterExpression `attribute_not_exists(awaitingPromotion) OR awaitingPromotion=:false`), sums across pages; `EncodeGSI1SK(score, businessID)` returns `%.4f#<id>` for natural lex sort. (b) Modified `lambdas/qualifier/main.go`: when `qualified=true`, looks up `Caps.MaxReviewQueueSize` + `CountActiveSlots`; if at/above cap, sets `BusinessUpdate.AwaitingPromotion=true` and SKIPS the `website.qualified` publish so preview gen doesn't pick it up. `updateBusinessStatus` reshaped to take a `BusinessUpdate` struct + writes gsi1pk + gsi1sk (priority-encoded via `queue.EncodeGSI1SK`) + awaitingPromotion atomically. (c) New `lambdas/backlog-promoter/main.go` — SQS consumer of `queue.slot.freed`. killswitch.WithKillSwitch(StageAudit) + idempotency.WithIdempotency at entry. Queries gsi1 BUSINESS#STATUS#qualified with FilterExpression awaitingPromotion=true, ScanIndexForward=false, Limit=1 → picks highest-priority backlog entry. Reads the latest Qualification via partition Query with begins_with(sk, QUAL#). Clears the awaitingPromotion flag with conditional `awaitingPromotion=:true` (idempotent: already-cleared returns success on CCFE). Publishes `website.qualified` with causationID=freed event but a fresh correlationID — the promoted business starts a new chain, not the freed-item's chain. Terraform: `backlog-promoter.tf` (Lambda 256MB/30s/provided.al2023 x86_64, SQS main with visibility 60s > timeout, EventBridge rule pattern on `queue.slot.freed`, queue policy scoped to rule ARN, ESM with ReportBatchItemFailures, inline IAM for SQS consume) + log group. Producer of queue.slot.freed lands in iter 6.x; until then the rule + Lambda sit quiescent. 16 + 5 + 8 + 3 tests across qualifier-backlog-gating, queue helper (sums + filter + pagination + lex-sort property), backlog-promoter (empty backlog no-op, happy path with correlation/causation invariant, qualification-missing skip, find/clear/publish error surfaces, idempotent replay, killswitch off). `/review-iteration 3.3` fired cost-idempotency-reviewer PASS + skeleton-pitfall-reviewer PASS.)*

## Medium Priority — Iteration 4: Spec Generator

- [x] **4.1** `VerticalStyleGuide` model + seed values. *(done 2026-05-14: `lambdas/pkg/style/style.go` models the row per 02-data-model.md § "Vertical Style Guide" — `Guide{Vertical, Tone, DoPhrases, DontPhrases, AntiPatterns, Palette{Primary, Neutral[]}, Version, CreatedAt, UpdatedAt, Etag}` keyed at `pk=STYLE#<lowercased-vertical>` `sk=PROFILE`. CRUD mirrors `pkg/targeting`: `Get` (returns `ErrNotFound` so callers can `errors.Is`), `GetOrDefault` (the spec-generator's iter-4.2 convenience — vertical-specific row if exists, else falls back to `STYLE#default`; case-insensitive against `DefaultVertical` constant), `List` (Scan filter on type), `Put` (validate + lowercase pk + fill timestamps/etag), `Update` (etag-checked read-then-write, auto-bumps Version if caller forgot — the spec-generator caches per-version, two writes must never share a version; preserves CreatedAt, rotates Etag). `SetNowFunc`/`SetEtagFunc` test hooks. Terraform `style-guides.tf` seeds two `aws_dynamodb_table_item` rows — "default" (plain-English, mild understatement, generic dontPhrases/antiPatterns + neutral dark palette) and "accountants" (mirrors the spec example: professional+trustworthy tone, MTD/Xero/ICAEW doPhrases, accountant-specific antiPatterns like "calculator-on-desk hero image", #0F4C81 primary) — both with `lifecycle.ignore_changes=[item]` so operator edits via /settings/style survive Terraform applies (same pattern as pipeline-settings.tf). 15 test cases: Validate required-field matrix, Put round-trip, Put lowercases pk, ErrInvalid on bad input, Get ErrNotFound on missing row, GetOrDefault prefers vertical-specific then falls back, empty-vertical uses default, Update etag mismatch, Update auto-bumps Version, Update respects caller-supplied Version override, Update rotates etag + preserves CreatedAt, List filters by type, attributevalue round-trip catches schema drift.)*
- [x] **4.2** `lambdas/spec-generator/` (Sonnet 4.6) with tool-use schema. *(split into 4.2a + 4.2b — stacked-PR pattern; both merged.)*
  - [x] **4.2a** spec.v1 prompt + schema + wrapper (pure-Go pkg). *(done 2026-05-14: three pieces. (a) `lambdas/pkg/schemas/spec_v1.go` — `SpecV1` Go struct with flat-union `SpecSection` carrying all kind-specific fields omitempty (invopop can't reflect oneOf+$defs cleanly), plus hand-written `SpecV1SchemaRaw` JSON Schema constant mirroring 07-bedrock-prompts.md verbatim (7 $defs, additionalProperties:false everywhere, minItems/maxItems/pattern), plus `ValidateSpecV1Structural` post-validator catching constraints-flag-false, section-count-outside-[4,8], unknown section type, testimonial-shaped section, password-in-copy. (b) `lambdas/pkg/prompts/spec_v1.go` — `prompts.SpecV1` (Sonnet 4.6, `produceSpec`, MaxTokens 3000, EstimateUSD 0.075, CacheTTL 90d) with pre-supplied Schema via new escape hatch in `prompts.New` (`if len(p.Schema) == 0 { reflect }` — prompts whose Go type can't reflect cleanly supply hand-written schema). Byte-stable snapshot at `pkg/prompts/testdata/spec_v1.snapshot.json`. (c) `lambdas/pkg/spec/spec.go` — `Run(ctx, Input, capUSD)` composes per-call system message (`SpecSystemPrefix + WrapBlock("style_guide", styleGuideJSON)`) by copying `prompts.SpecV1` and overriding `System` on the copy, then calling `prompts.Invoke` so the `PostValidate` hook fires through the normal framework path (not Apply+manual-validate which would silently drop validation on refactor). Cache key built from `(businessID, auditID, styleGuide.Version)` per spec. 11 + 5 + 14 tests covering schema validator + 7 adversarial structural cases, prompt config + system + snapshot, Run happy/cache-hit/cache-key-derivation/style-version-changes-key/input-validation/composeSystem/bedrock-error and 5 end-to-end adversarial cases (testimonial, constraints-flag-false, too-few-sections, password-in-copy, unknown-section-type). `bedrock-prompt-reviewer`: PASS after 3-item fix pass (added cost-class comment, restructured Run to flow through Invoke, added 4 Run-level adversarial tests).)*
  - [x] **4.2b** spec-generator Lambda consumer of `website.qualified` events + Terraform. *(done 2026-05-14: SQS-driven consumer of `website.qualified`. `handle` wraps the batch in `killswitch.WithKillSwitch(StagePreview)` (spec-generator rolls up to preview per StageMap — operators pause spec gen via `previewEnabled`); `processRecord` wraps the per-record work in `idempotency.WithIdempotency("spec-generator", env.EventID)`. Per-record: loads Business + Audit rows; resolves `style.GetOrDefault(business.vertical)` (falls back to STYLE#default); looks up `CapUSD(StagePreview)` for the Bedrock cap (preview rolls up to Budgets.DailyBedrockUsd same pool as audit; cost.Assert uses the prompt's Stage "spec" as ledger key); runs `spec.Run` which composes the per-call system message with the style guide block and flows through `prompts.Invoke(SpecV1)` so PostValidate fires through the framework path. Builds `spec.Input` via `buildSpecInput` projection (squeezes qualitative summary + dedupes issue types, tolerates absent qualitative block — leaves ExtractedContent empty for v1, iter 6.x adds structured services/contact extraction). Persists `SpecRow` per 02-data-model.md (status=draft, version=1, modelId=Sonnet 4.6, promptId=spec.v1, full SpecV1 in `content`). Publishes `spec.generated` with `{businessId, specId, tokensIn=0, tokensOut=0, modelId}` (token counts are best-effort zeros pending iter 9.x analytics surface). Correlation + causation propagated from inbound envelope. Terraform: `spec-generator.tf` adds Lambda (512MB, 180s timeout, provided.al2023 x86_64), SQS main queue (visibility 240s > Lambda timeout, retention 4d, maxReceiveCount=3 → dlq["spec-generator"]), EventBridge rule `website-qualified-to-spec-generator` with retry_policy + dead_letter_config, queue policy scoped to rule ARN, ESM with `batch_size=2` (small because Sonnet calls are expensive — small batches limit single-poison-record blast radius) + ReportBatchItemFailures, inline IAM for SQS consume on the new queue, spec-generator log group. 11 test cases covering happy path with correlation/causation invariants + Spec row schema, buildSpecInput dedupes issue types + tolerates no-qualitative-block, missing Business + missing Audit short-circuits, RunSpec/publish error surfaces, idempotent replay, killswitch off, bad-body BatchItemFailure. `/review-iteration 4.2b`: cost-idempotency-reviewer PASS (kill switch right stage, idempotency per-record, cost cap chain verified end-to-end to cost.Assert, polite-fetch N/A, 90d TTL written on cache miss, IAM least-privilege), skeleton-pitfall-reviewer PASS (#3/#5/#6/#10 all clean, visibility>timeout, DLQ + retry_policy on EB target, nosemgrep marker, runtime+arch, no math/rand). Iter 4.2 (spec generator) fully closed.)*
- [x] **4.3** Spec review UI + Approve / Edit / Reject capture path emitting `feedback.captured`. *(split into 4.3a + 4.3b — stacked-PR pattern; both merged.)*
  - [x] **4.3a** Backend BFF (`lambdas/api-specs/`) + `pkg/feedback` helper. *(done 2026-05-14: two pieces. (a) `lambdas/pkg/feedback/feedback.go` — `Capture(ctx, CaptureInput, publisher)` writes the Feedback row per 02-data-model.md (`pk=FEEDBACK#<vertical>`, `sk=<createdAt>#<id>`, gsi2pk=`FEEDBACK#VERTICAL#<vertical>`, gsi2sk=`<subject>#<createdAt>`) and publishes `feedback.captured` in one call. Validation matrix on Subject (closed set: audit/qualification/spec/website/email) + Action (closed set: approve/reject/edit/regenerate) + required fields. Empty vertical falls back to "default" partition. nil publisher writes row but skips event. SetNowFunc/SetIDFunc test hooks. 9 test cases covering happy path, vertical fallback, nil-publisher path, validation matrix, DDB error surfaces, publish error surfaces but row still persisted, DDB attribute-name verification. (b) `lambdas/api-specs/main.go` — operator-only BFF for the iter 4.3 `/queue/[id]` page. Five routes: GET /candidates/{businessId} (returns business + latest spec), GET /candidates/{businessId}/specs (list newest-first), PATCH /candidates/{businessId}/specs/{specId} (edit content + bump version + rotate etag — content validated by `schemas.ValidateSpecV1Structural` before write — emits `feedback.captured` with original+edited payloads), POST .../approve and POST .../reject (flips status to approved/rejected with approvedBy=sub + approvedAt; publishes `spec.approved` / `spec.rejected` first then `feedback.captured` as audit trail). 409 on non-draft decisions; 400 on invalid content/missing IDs; 404 on missing business/spec; 403 when not operator; 405 on unknown method. Auth helper `auth.Sub(req)` extracts cognito sub for the actor field. 11 test cases covering all five routes + 403/404/400/405/409 paths + two-events-on-approve invariant + etag rotation + version bump. Terraform: `api-specs.tf` adds Lambda (256MB, 15s, provided.al2023 x86_64), API GW integration + 5 routes (all JWT-authorized via cognito authorizer), log group. `/review-iteration 4.3a`: cost-idempotency-reviewer PASS (HTTP BFF — killswitch/idempotency/cost-cap/polite-fetch/cache-TTL all N/A), skeleton-pitfall-reviewer PASS (#3/#5/#6/#7/#8/#10 clean, nosemgrep markers correct, runtime+arch correct, no math/rand).)*
  - [x] **4.3b** Frontend `/queue/[id]` page wiring the BFF. *(done 2026-05-14: `frontend/src/pages/Candidate.tsx` is the operator's spec review surface. `useParams<{businessId}>` resolves the candidate; `useEffect` loads `getCandidate(businessId)` from the BFF on mount + cleanup-via-cancellation when the businessId changes. Renders: business header (name · domain · vertical · location · status badge); a structured SpecSummary panel showing tone/positioning/palette swatches/SEO title/section type trail; a JSON editor (textarea, read-only until focused so accidental clicks don't dirty the state, toggles `editing` flag for the Save button's enable gate); a notes textarea captured with the feedback event; an action bar with Approve / Save edits / Reject buttons. Disabled state covers: in-flight request, spec missing, spec status != "draft" — the "Spec is <status>. Actions disabled." hint surfaces when a decision is already recorded. JSON parse errors surface inline before sending. Server errors from any action surface in a dedicated `action-error` testid pane. `refreshSpec` after every successful action replaces the local spec + clears editing state + clears notes — the page never silently round-trips stale data. Out-of-scope-for-4.3 deferred to later iters (original-site iframe + preview iframe → iter 5 R2/Worker; access strip → iter 5; contact panel → iter 6.x enrichment) documented in the page header comment. `frontend/src/api.ts` extended with SpecV1-mirror types (SpecBrand/SpecPalette/SpecSection/SpecSubItem/SpecBadge/SpecSEO/SpecConstraints/SpecV1Content/Spec/CandidateBusiness/CandidateResponse) + four BFF helpers: `getCandidate`, `patchSpec`, `approveSpec`, `rejectSpec` (the last two delegate to a shared `decideSpec(decision)` inner function so the URL shape stays in lockstep). Route wired in `main.tsx` at `/queue/:businessId` under the AuthGuard. 9 new test cases (58 total passing): header + summary + editor rendered, no-spec-yet path, GET error, Approve disables and shows status hint, Reject sends notes in body, Save edits sends edited JSON + refreshes to v2, Save surfaces JSON parse error before sending, approved-status disables buttons, 502 from Approve surfaces in action-error pane. `npm run format` + `npm run lint` + `npm run build` clean. Iter 4.3 fully closed.)*

## Medium Priority — Iteration 5: Component Site Generator + R2 Publish + Passcode

- [x] **5.1** Component renderers in `lambdas/pkg/components/` (Go templates). *(done 2026-05-14: `lambdas/pkg/components/components.go` exposes `RenderSection(s schemas.SpecSection, opts RenderOptions) (string, error)` that branches on `s.Type` and calls one of seven per-kind renderers (Hero / Services / About / Trust / FAQ / CTA / Contact), plus `RenderFooter(FooterInput)` for the always-present footer the generator appends (the model never produces a Footer section). All templates use `html/template` so user-controlled fields are HTML-escaped by default. Templates are inline string constants compiled once at package init via `mustParse`. `RenderOptions` carries the two cross-section knobs the generator needs but the SpecSection doesn't: `VerifiedBadges map[string]bool` (Trust drops any badge whose label isn't in the verified set — nil means "drop all" so an unverified Trust section emits empty rather than leaking) and `ImageURLFor func(prompt) string` (Hero renders a real `background-image` URL when set, otherwise a `data-image-prompt` placeholder div the generator post-processes). CSS classes use a single `s-` prefix so the generator's stylesheet can target `[data-section="hero"]` etc. without coupling per-class. Documented renderer guarantees from 07-bedrock-prompts.md inline: testimonial stripping (caught upstream in prompts.SpecV1 PostValidate), badge verification, image-prompt swap, palette-contrast pass (these last two delegate to the iter 5.2 generator). 22 test cases: Hero shape + HTML-escape + image-prompt placeholder + ImageURLFor resolves + missing-cta error; Services items + empty-items error; About paragraph + empty-paragraph error; Trust drops-unverified + all-dropped-empty + nil-verifiedBadges-drops-all; FAQ; CTA shape + missing-button error; Contact omits-absent-fields + URL-escapes tel: + renders-all-when-present; Footer with/without opt-out; unknown-section-type error; round-trip smoke test over `schemas.AllowedSectionTypes` confirming every kind renders.)*
- [x] **5.2** `lambdas/generator/` consumes Spec → static HTML bundle. Lighthouse ≥ 90 in CI. *(done 2026-05-14: two pieces. (a) `lambdas/pkg/sitebundle/sitebundle.go` — `Render(Input) (string, error)` assembles a complete self-contained HTML document from a SpecV1: sections rendered via `components.RenderSection` then a `Footer` appended; inline CSS using CSS variables for the palette so no extra HTTP requests, no asset-resolution pass at publish time, Lighthouse measures pure document performance. Validates the spec via `ValidateSpecV1Structural` before rendering; surfaces clear per-section error messages. Trust with all-dropped badges → section silently skipped (whole section gone, no empty hole). Passes through `VerifiedBadges` + `ImageURLFor` from `components.RenderOptions`. 8 test cases: happy path with all metadata/title/description/CSS vars/section trail/footer, palette CSS vars, Trust drop, verified-badge keep, image URL resolve, invalid spec rejected, footer year+optout, document balanced root tags. (b) `lambdas/generator/main.go` — SQS consumer of `spec.approved`. handle wraps in `killswitch.WithKillSwitch(StagePreview)`; `processRecord` wraps in `idempotency.WithIdempotency("generator", env.EventID)`. Per-record: load Business + Spec; defensive guard `spec.Status == "approved"` (race-y approve→reject won't generate from stale state); render via `sitebundle.Render`; PutObject to S3 at `generated/<websiteId>/index.html` (publisher in iter 5.3 copies S3 → R2); persist Website row per 02-data-model.md (`pk=BUSINESS#<id>, sk=WEBSITE#<id>, status="generated", r2Prefix="sites/<id>/", specId, etag`); publish `website.generated` with `{businessId, specId, websiteId, s3Key}`; correlation + causation propagated from inbound envelope. 11 test cases: happy path with full row + event verification, non-approved-spec skip, missing business/spec skip, S3 error surfaces (no publish), publish error surfaces (row already persisted), idempotent replay, killswitch off, bad-body BatchItemFailure. Terraform: `generator.tf` adds Lambda (512MB/60s/provided.al2023 x86_64), SQS main (visibility 120s > timeout, maxReceiveCount=3 → `dlq["generator"]`), EventBridge rule `spec-approved-to-generator` with retry_policy + dead_letter_config, queue policy scoped to rule ARN, ESM with `batch_size=3` + ReportBatchItemFailures, inline IAM for SQS consume, generator log group. `/review-iteration 5.2`: cost-idempotency-reviewer PASS (kill switch right stage, idempotency per-record, no paid calls / scrape / cache TTL to verify, IAM least-privilege within the shared role's existing grants), skeleton-pitfall-reviewer PASS (#3/#5/#6/#8/#10 clean, visibility>timeout, EB target retry+DLQ, nosemgrep, runtime+arch, no math/rand).)*
- [x] **5.3** `lambdas/publisher/`: upload to R2; **issue passcode** (8-char Crockford-Base32); write hash to DynamoDB and Workers KV; KMS-encrypt cleartext into `Website.passcodeCipher`; set `passcodeRevealableUntil = now + 7d`. Publishes `website.published` (no cleartext). *(done 2026-05-14: SQS consumer of `website.generated`. `handle` wraps in `killswitch.WithKillSwitch(StagePreview)`; `processRecord` wraps in `idempotency.WithIdempotency("publisher", env.EventID)`. Per-record: load Business + Website; defensive guard `Status=="generated"`; read HTML from AWS S3 (blobs bucket via aws.Config default creds); upload to Cloudflare R2 at `sites/<websiteId>/index.html` via R2's S3-compatible API using a separate `s3.NewFromConfig` with `BaseEndpoint = https://<account>.r2.cloudflarestorage.com` + static R2 credentials from SSM (region "auto"); call `passcode.Generate` → 8-char Crockford-Base32 cleartext; `passcode.Hash` with salt from SSM; `passcode.EncryptCleartext` via KMS (key from existing `aws_kms_key.passcode_cleartext`; key policy already grants the role Encrypt); `passcode.KVWriter.Put` writes hash to Cloudflare Workers KV via REST API (key=`passcode:<websiteId>`, value=hash, metadata={businessId, websiteId}); update Website row with passcodeHash + passcodeCipher + passcodeRevealableUntil = now+7d + previewUrl=`<base>/sites/<id>` + status=`published`; publish `website.published` with `{businessId, websiteId, previewUrl, passcodeIssued, passcodeRevealableUntil}` — **NO cleartext in the event payload, no cleartext in logs**. Test asserts the published envelope JSON does not contain the cleartext string. Cleartext lives only in the `code` local variable; hashed+encrypted before any logger reach; never assigned to any struct field. Terraform: `publisher.tf` adds Lambda (512MB/90s/provided.al2023 x86_64), SQS main (visibility 180s > timeout, maxReceiveCount=3 → DLQ), EventBridge rule `website-generated-to-publisher` with retry_policy + dead_letter_config, queue policy scoped to rule ARN, ESM with batch_size=3 + ReportBatchItemFailures, inline IAM for SQS consume, 7 new SSM SecureString placeholders (R2 access key id + secret, Cloudflare account ID + KV namespace ID + API token, passcode salt, preview URL base) — same placeholder-then-ignore-changes pattern as discover.tf providers (operator populates each out-of-band after first apply). 11 test cases: happy path (R2 key + body + KV key/value/metadata + Website row + revealableUntil = now+7d + previewUrl + status=published + published event with passcodeIssued=true), cleartext-not-in-event invariant, non-generated-status skip, missing business/website skips, R2/encrypt/KV error surfaces with correct rollback state, publish error after row updated (SQS retry redoes publish), idempotent replay, killswitch off, bad-body BatchItemFailure. `/review-iteration 5.3`: cost-idempotency-reviewer PASS (kill switch right stage, idempotency per-record, no paid calls, cleartext confinement verified — never logged, never in struct field, never in event), skeleton-pitfall-reviewer PASS (#3/#4/#5/#6/#10 all clean, visibility>timeout, EB target retry+DLQ, nosemgrep markers, runtime+arch, no math/rand). One IAM-least-privilege debt noted: shared `lambda_api` role has `kms:Decrypt` which the publisher doesn't use (legitimate for email-draft); pre-existing shared-role pattern, not introduced by this PR.)*
- [x] **5.4** Worker (already provisioned in 0.C) extended to enforce passcode flow with rate-limiting; signed cookie; constant-time compare; revocation propagation < 60s. *(done 2026-05-15, merged via PR #46. Rate-limiting (zone-level ruleset), HMAC-SHA256 signed cookie scoped to `/sites/<id>/`, and constant-time compare were already in place from 0.D.2. This iter adds **revocation propagation <60s**: `worker/src/index.ts` `passcodeStillIssued(env, websiteId)` checks the KV `passcode:<websiteId>` entry still exists on EVERY cookie-authenticated request to `/sites/*` (serveAsset) and `/screenshots/*` (serveScreenshot), not just on passcode submit — closing the gap where a valid signed cookie granted access for its full 24h TTL after the operator revoked the KV entry. Per-isolate `revocationCache` Map bounds it to one KV read per `REVOCATION_TTL_SECONDS=60` per website (hot previews don't pay a KV read per request). Revoked → serveAsset returns the passcode form with "This preview has been revoked."; serveScreenshot returns 403. `passcodeStillIssued` + `resetRevocationCacheForTests` exported for deterministic tests. 18 worker tests pass (12 existing + 6 new: KV present/absent/empty-string, cache-bounds-KV-reads, cookie+revoked→form, cookie+revoked-screenshot→403). worker lint/typecheck/prettier clean; Go `pkg/passcode` tests green. **argon2id swap DEFERRED as a tracked follow-up** — documented inline in `worker/src/passcode.ts` AND `lambdas/pkg/passcode/passcode.go`: the Go side picks up `golang.org/x/crypto/argon2` cleanly (x/crypto v0.32.0, no go-directive bump) but the Cloudflare `cloudflare/vitest-pool-workers` test runtime blocks dynamic `WebAssembly.compile()` that `hash-wasm` (and every Workers-side argon2 lib) depends on — production Workers DO support WASM. Both sides reverted to the cross-pinned SHA-256 (`SHA-256("<passcode>|<salt>")` hex; vector `65934f82…` pinned on both sides) and stay in lockstep until either a wrangler `wasm_modules` preload binding or a worker-pool-free test path lands. `lambdas/go.mod` churn in the commit is benign — `go mod tidy` promoting the iter-5.3 publisher's s3/lambda/credentials deps from indirect→direct. **argon2id swap remains a tracked follow-up** — blocked on a wrangler `wasm_modules` preload or worker-pool-free test path; rationale inline in `worker/src/passcode.ts` + `lambdas/pkg/passcode/passcode.go`.)*
- [x] **5.5** Screenshot job via Cloudflare Browser Rendering API with operator-mode bypass token. *(done 2026-05-15 — split 5.5a + 5.5b, stacked-PR pattern. **5.5a** (PR #49): operator-mode bypass in the Worker — `worker/src/passcode.ts` `signOpToken`/`verifyOpToken` produce `<websiteId>.<exp>.<sig>` with `sig=HMAC-SHA256(salt,"op:<id>.<exp>")`; the `op:` prefix domain-separates it from the cookie signature so neither replays as the other; `serveAsset` honours a valid `?op=` (sets the session cookie, serves from R2) but stays gated on `passcodeStillIssued` so the bypass can't outlive a revoked preview; "one-time" = 120s TTL (stateless, server-minted right before the Browser Rendering call, never logged/persisted), not a KV nonce. **5.5b** (PR #50): `lambdas/screenshotter/` SQS consumer of `website.published` — mints the op token, captures desktop (1280×800) + mobile (390×844) via the Cloudflare Browser Rendering REST API, uploads both PNGs to R2 at `screenshots/<id>/<size>.png`, patches `Website.screenshots` via a targeted `UpdateItem` (no full-row clobber). `lambdas/pkg/passcode.SignOpToken` is byte-cross-pinned with the Worker (shared HMAC vector pinned in BOTH suites: `passcode_test.go` crossPinned* ↔ worker test). Entry `killswitch.WithKillSwitch(StagePreview)`; `idempotency.WithIdempotency(eventId)`; the paid Browser Rendering pair wrapped in one `cost.WithCostCap` (estimate/actual 2×$0.0005, cap `killswitch.CapUSD(preview)`), no spend on failure; op token is a bearer capability — never logged, error path logs HTTP status only; authenticated API → plain `http.Client` (not politefetch). Worker `serveScreenshot` route extended to the data-model sizes `desktop|mobile` (legacy thumb/small/medium/large retained). `killswitch.StageMap` gains `screenshotter→preview`. Terraform `screenshotter.tf` (Lambda 512MB/120s, SQS visibility 240>timeout, EB `website.published`→SQS w/ retry+DLQ, scoped queue policy, ESM ReportBatchItemFailures, inline SQS-consume IAM, new `/cloudflare/browser_rendering_token` SSM placeholder under the already-allowlisted `/cloudflare/*`); `sqs.tf`+`lambda-log-groups.tf` add the screenshotter DLQ + log group. No outbound event — 03-events.md defines none; the iter-6 queue reads the Website row. `/review-iteration 5.5`: skeleton-pitfall-reviewer PASS (all 20 clean vs publisher.tf pattern), cost-idempotency-reviewer PASS (all 5 mandatory rules). Go `go test ./...`+golangci-lint clean; 30 worker tests + cross-pin vector green.)*
- [x] **5.6** Site preview Approve/Reject/Regenerate captures `feedback.captured`. Regenerate-site issues a fresh passcode (revoke old). *(done 2026-05-15 — split 5.6a + 5.6b, sequential PRs to main (NOT stacked, per the 5.5 merge-ordering lesson). **5.6a** (PR #52, merged): new operator-only `lambdas/api-website/` BFF mirroring api-specs — `GET /candidates/{businessId}/website` returns a SANITISED `websiteView` (passcodeHash/passcodeCipher never leave the BFF), `POST .../approve` + `/reject` flip status (published-only guard → 409) + publish `website.approved` / `website.rejected_after_review` + `feedback.Capture(subject=website)`; operator-group gated. `terraform/api-website.tf` (shared lambda_api role, API GW + JWT routes) + `api_website` log group. Frontend: `api.ts` website client + sanitised `WebsiteView`; Candidate `/queue/[id]` gains a Site-preview section — AccessStrip (URL + cleartext-window countdown), screenshot thumbnails, Approve/Reject buttons. **5.6b** (this PR): `handleRegenerateSite` emits `website.regenerate.requested` {businessId,specId,websiteId} + feedback(action=regenerate), parks status="regenerated"; the generator gains an EB rule for that event and reuses the carried websiteId (re-renders the existing approved Spec — generator never calls Bedrock, so regenerate is a pure re-render — onto the same id; publisher then overwrites the R2 prefix + KV key, invalidating the old passcode and issuing a fresh one). `handleRegeneratePasscode` rotates in place: new code → Hash → KV overwrite (`passcode:<id>` replaced → old cleartext instantly invalid) → KMS-encrypt → Website row (passcodeRevealableUntil=now+7d, passcodeRevokedAt cleared); cleartext NEVER logged/returned/evented (sentinel-string + row-sweep tests). api-website gains the publisher's passcode/KMS/KV SSM wiring (reuses publisher.tf data sources; kms:Encrypt via existing key policy). Frontend: Regenerate-site button + AccessStrip onRegenerateCode→regenerate-passcode. Also added `terraform/NAMING.md` (acronym map + naming convention) and acronymised the new EB rule `web-regen-to-gen` per it, fixing a pitfall-#8 name-length flag (applied to new names only — no destructive retro-renames). `/review-iteration 5.6`: skeleton-pitfall-reviewer PASS (after the #8 name-length fix), cost-idempotency-reviewer PASS (sync-BFF: kill-switch/idempotency/cost-cap N/A per api-specs precedent; cleartext-confinement + sanitisation verified end-to-end; generator idempotency/spec-guard unchanged). Go `go test ./...`+golangci-lint clean; frontend typecheck+eslint+prettier+58 tests; terraform fmt clean.)*

## Medium Priority — Iteration 6: Review Queue

- [x] **6.1** `lambdas/api-queue/` (`GET /queue`, paginated by priority). *(done 2026-05-15, PR #54 → main. New operator-only sync BFF mirroring api-specs/api-website. `GET /queue?status=&limit=&cursor=`: `auth.IsOperator` gate (403) then GET-only (405); `status` whitelisted (awaiting_review default; also qualified/approved/rejected_after_review/regenerate_requested) → 400 on anything else so a typo can't select an unexpected partition; `limit` validated + capped at maxLimit=100 (default 50); gsi1 Query (`IndexName=gsi1`, `gsi1pk="BUSINESS#STATUS#<status>"`, `ScanIndexForward=false` → highest priorityScore first); priorityScore parsed from the `%.4f#<id>` gsi1sk; opaque base64-JSON cursor round-trips the gsi1 LastEvaluatedKey (all-String key attrs), malformed → 400 (no panic/500). Response is Business-card fields only — no passcode/PII beyond the public domain. terraform/api-queue.tf (shared lambda_api role, read-only; API GW + JWT route `GET /queue`) + api_queue log group. 11 table-driven tests (auth/method, gsi1 query shape, status whitelist, limit cap, cursor round-trip into ExclusiveStartKey, invalid cursor, query-error→500). `/review-iteration 6.1`: skeleton-pitfall-reviewer PASS (all 20 clean; name lengths within limits per terraform/NAMING.md), cost-idempotency-reviewer PASS (kill-switch/idempotency/cost-cap correctly N/A for a sync read-only BFF; all 6 substantive correctness items verified). go test ./... + golangci-lint clean; terraform fmt clean. UI (6.2) + access strip (6.3) follow.)*
- [x] **6.2** Queue UI with thumbnails, filters, action bar, daily-cap awareness. *(done 2026-05-15, PR #55 → main. `frontend/src/pages/Queue.tsx` rewritten from the 0.G.4 stub: consumes `getQueue()` (api.ts client + `QueueItem`/`QueueResponse` types added) — server-ordered by priorityScore desc via gsi1. Card grid; each `QueueCard` lazy-loads its website (bounded by the queue cap) for the desktop screenshot thumbnail + previewUrl + websiteId. Client-side filters: vertical (dropdown of distinct verticals), location (contains), min-priority (range slider). Inline action bar reusing the iter-5.6 api-website BFF on `lastWebsiteId`: Approve / Regenerate / Reject (Reject opens a reason picker — the 04-feedback-loops Loop-1 reason list — then confirms); resolved cards drop out of the list and bump the session counter. Daily-cap awareness: `getSettings().caps.maxReviewQueueSize` shown as "queue cap N" + "<n> reviewed this session" (session counter; a persisted daily metric is iter-11 analytics — labelled so it doesn't overclaim). Cursor pagination via "Load more". Links to the `/queue/:businessId` detail page (Candidate.tsx) for the full spec editor. Page heading renders during load/error too (keeps the route smoke test stable). **Snooze deferred** — no `awaitingReviewUntil` endpoint exists yet (out of 6.2 scope; noted inline). No backend/terraform changes, so no review agents apply (frontend-only diff, per the worker-only precedent); self-reviewed. Gates: prettier + eslint + tsc clean; 63 frontend tests pass (10 files incl. new Queue.test.tsx — render/order/cap, vertical filter, approve-removes-and-counts, reject-reason flow, load error). 6.3 (access strip on `/queue/[id]`) follows.)*
- [x] **6.3** Access strip on `/queue/[id]`: copy URL, copy code, show/hide, regenerate code, cleartext-window countdown. *(done 2026-05-15, PR #56 → main. Completes Iteration 6. copy-URL / regenerate-code / countdown / show-hide were wired in 5.6a+5.6b; 6.3 adds the missing **passcode cleartext reveal**. Backend: new `POST /candidates/{businessId}/website/{websiteId}/reveal-passcode` on api-website + `Decrypt` added to `passcodeOps`/`defaultPasscodeOps` (`passcode.DecryptCleartext` via the existing KMS client). Operator-gated; server-side guards mirror the UI and run BEFORE decrypt: empty `passcodeCipher` → 409, `passcodeRevokedAt` set → 409, past `passcodeRevealableUntil` → 409. KMS-decrypts and returns `{"passcode": "<cleartext>"}` — the cleartext appears ONLY in the response body (the one sanctioned channel per 10-quality-rules.md, alongside the email body): never logged (success + decrypt-error log lines carry websiteId only; KMS error detail not echoed to client), never evented, never persisted, no feedback row. **No terraform change** — `kms.tf` already grants `kms:Decrypt` to the shared `lambda_api` role and `PASSCODE_KMS_KEY_ID` is already in `api-website.tf` (both from 5.6b). Frontend: `api.ts revealPasscode()`; `AccessStrip` gains optional `onReveal` + a "Reveal code" button (shown only while the window is open and nothing revealed yet; backward-compatible — existing usages unaffected); `Candidate.tsx` holds the revealed cleartext in component memory only (`revealedPasscode`), passes it as `passcode` + wires `onReveal`/`onCopyCode`, and **wipes it on every state-changing action** (approve/reject/regenerate-site/regenerate-passcode) — never localStorage/sessionStorage/URL. `/review-iteration 6.3`: skeleton-pitfall-reviewer PASS (no terraform needed — confirmed); cost-idempotency-reviewer PASS after two fixes (added `TestHandle_RevealPasscode_409WhenRevoked`; clear `revealedPasscode` on approve/reject/regenerate-site too, not just rotation). Gates: go test ./... + golangci-lint clean; frontend prettier+eslint+tsc clean; 66 frontend tests (10 files, +reveal cases); api-website Go tests incl. happy-path cleartext non-leakage sweep over events+DDB + all three 409 guards.)*

## Medium Priority — Iteration 7: Email Draft Generator

- [x] **7.1** `EmailToneProfile` model + seed values. *(done 2026-05-15, PR #57 → main. New `lambdas/pkg/tone/tone.go` models the row per 02-data-model.md § "EmailToneProfile" — `Profile{Vertical, SubjectPatterns[], OpenerPatterns[], ProhibitedPhrases[], Signature, OptOutLine, Version, CreatedAt, UpdatedAt, Etag}` keyed `pk=EMAIL_TONE#<lowercased-vertical>` `sk=PROFILE` `type=EmailToneProfile`. CRUD mirrors `pkg/style` exactly: `Get` (ErrNotFound sentinel), `GetOrDefault` (vertical-specific else `EMAIL_TONE#default`, case-insensitive — the iter-7.2 email-draft convenience), `List` (Scan filter on type — /settings/email-tone UI + iter-9 tuner), `Put` (validate+lowercase pk+fill timestamps/etag), `Update` (etag-checked read-then-write, auto-bumps Version so the email.v1 7-day cache key changes; preserves CreatedAt, rotates Etag). `Validate` enforces the fields the email.v1 prompt + post-validator depend on: vertical, optOutLine (post-validator rejects drafts missing it verbatim), ≥1 subjectPattern, ≥1 openerPattern, version≥1. `SetNowFunc`/`SetEtagFunc` test hooks. Terraform `email-tone-profiles.tf` seeds two `aws_dynamodb_table_item` rows — "default" + "accountants" (mirrors the spec example + pkg/tone fixtures; prohibitedPhrases carries the project-wide exaggeration ban + "password"; signature is the techar.ch placeholder consistent with style-guides.tf) — both `lifecycle.ignore_changes=[item]` so operator/tuner edits survive applies (same pattern as style-guides.tf/pipeline-settings.tf). 16 test cases: Validate required-field matrix + good, Put round-trip, Put lowercases pk, ErrInvalid, Get ErrNotFound, GetOrDefault prefers/falls-back/empty-uses-default, Update etag-mismatch / auto-bump / caller-bump / rotates-etag-preserves-createdAt, List filters by type. `/review-iteration 7.1`: skeleton-pitfall-reviewer PASS (all 20 clean — only `aws_dynamodb_table_item` on the existing table, no new table/GSI/SSM/IAM/singleton; signature literal matches the 4.1 precedent). go test ./... + golangci-lint clean; terraform fmt clean. 7.2 (email-draft Lambda + email.v1 prompt) + 7.3 (email review UI) follow.)*
- [x] **7.2** `lambdas/email-draft/` (Haiku 4.5). KMS-decrypts `passcodeCipher`; calls Bedrock with cleartext via prompt; cache key uses hash; `{{PASSCODE}}` placeholder substitution. Post-validator requires both URL and passcode literals. *(split into 7.2a + 7.2b — sequential PRs to main, NOT stacked, per the 5.5 merge-ordering lesson; both merged.)*
  - [x] **7.2a** `email.v1` prompt + schema + post-validator + tests (pure-Go pkg). *(done 2026-05-15, PR #58 → main. `lambdas/pkg/schemas/email_v1.go`: `EmailV1{Subject(≤80), Body(≤1500), WordCount(60–200)}` reflected schema (jsonschema tags, no hand-written schema — flat struct). Post-validator split: **intrinsic** `ValidateEmailV1Structural` (wired to `prompts.EmailV1.PostValidate`, runs every Bedrock call) — non-empty subject/body, wordCount 60–200, no "password", `{{PASSCODE}}` placeholder exactly once; **context-dependent** `ValidateEmailV1Content(e, EmailV1Context{PreviewURL,OptOutLine,ProhibitedPhrases})` (the iter-7.2b Lambda runs it with runtime data) — previewURL exactly once, optOutLine verbatim, no per-vertical prohibited phrase, + 10-quality-rules Rule-1 invented-fact regexes (%, since-YYYY, N years/customers/clients, guaranteed/award/best-in-class/…). `lambdas/pkg/prompts/email_v1.go`: `EmailV1` Prompt[schemas.EmailV1] — Haiku 4.5, forced tool `produceEmailDraft`, MaxTokens 600, Stage email, EstimateUSD 0.005, CacheTTL 7d; `emailSystemPrefix` mirrors 07-bedrock-prompts.md § email.v1 verbatim + SafetyRulesBlock + the spec-mandated `{{PASSCODE}}` literal-token instruction (the model never sees real cleartext; cache key = sha256(business+website+contact+tone.version), no cleartext input — reconciles spec rule (c) with that section's caching paragraph; documented in code since `.ralph/specs/` is read-only). `EmailSystemPrefix` exported so 7.2b composes `+ WrapBlock("email_tone", …)` per-call. Byte-stable snapshot at `pkg/prompts/testdata/email_v1.snapshot.json`. Tests: prompts config/system-blocks/snapshot/PostValidate-accept + 7 adversarial; schemas reflected-schema round-trip + structural-reject matrix + 10 content-reject cases (incl. all four invented-fact patterns). `/review-iteration 7.2a`: bedrock-prompt-reviewer FAIL→PASS after fixing the one hard item (added adversarial tests for the N-customers + guarantee/award invented-fact patterns) + 2 WARNs (tightened the cache-key comment; documented the rule-(c) placeholder reconciliation in code as the spec doc itself can't be edited here). go test ./... + golangci-lint clean. 7.2b (email-draft Lambda consumer + KMS-decrypt + cache + Terraform) follows.)*
  - [x] **7.2b** `lambdas/email-draft/` consumer + KMS-decrypt + cache + Terraform. *(done 2026-05-15, PR #59 → main. New `lambdas/pkg/emaildraft/emaildraft.go` (mirrors pkg/spec): `Run(ctx, Input, capUSD)` builds the cache key `bedrock.CacheKey(email.v1, HashInputs(businessID, websiteID, contactID, tone.Version))` — **passcode is never a key segment or model input**; composes per-call system = `EmailSystemPrefix + WrapBlock("email_tone", toneJSON)`; user blocks business/contact/audit/preview_url; `prompts.Invoke` (intrinsic PostValidate) then `schemas.ValidateEmailV1Content` on the placeholder body (preview-URL once, opt-out verbatim, prohibited phrases, invented-fact regexes). New `lambdas/email-draft/main.go`: SQS consumer of `website.approved` (authoritative routing table; 09-iterations names it "outreach.email.requested" — reconciliation documented in the package doc since `.ralph/specs/` is read-only). Entry `killswitch.WithKillSwitch(StageOutreach)`; `idempotency.WithIdempotency(eventId)`; loads Business + approved Website + latest Audit + latest Contact + `tone.GetOrDefault(vertical)`; **wiped-cipher guard** (empty `passcodeCipher` → clear operator-actionable error log + NO draft + nil, no DLQ churn — matches the iter-7 acceptance); KMS-decrypts `passcodeCipher` (cleartext in one local only); `capUSD=killswitch.CapUSD(StageOutreach)`→Budgets.DailyEmailUsd; `emaildraft.Run`; substitutes cleartext for `{{PASSCODE}}` (the one body touch — `EmailDraft.body` is the sanctioned at-rest location per 02-data-model.md); persists `EmailDraft` (status=draft, modelId Haiku, promptId email.v1); publishes `outreach.email_ready` {businessId,draftId,websiteId,contactId,wordCount} — **NO body/cleartext**; completion log omits subject/body. Terraform `email-draft.tf` mirrors spec-generator.tf + the publisher's `PASSCODE_KMS_KEY_ID` env (kms:Decrypt already granted on lambda_api since 5.6b; Haiku in bedrock-iam.tf; DLQ `dlq["email-draft"]` already in sqs.tf) — EB rule `website.approved`→SQS (visibility 240>timeout 120, retry+DLQ, ArnEquals-scoped queue policy, ESM ReportBatchItemFailures, scoped SQS-consume inline IAM); `email_draft` log group + depends_on. Tests: pkg/emaildraft (happy/cache-key/content-reject/required-inputs) + email-draft (cleartext-substituted-and-not-leaked sentinel sweep over event JSON, wiped-cipher→no-draft, missing-rows skip, killswitch-off). `/review-iteration 7.2b`: cost-idempotency-reviewer PASS (all 5 checks; cleartext confinement airtight end-to-end), skeleton-pitfall-reviewer PASS (all 20 clean; names within limits per NAMING.md). go test ./... + golangci-lint clean; terraform fmt clean.)*
- [x] **7.3** Email review UI; capture path with passcode redacted in `originalPayload`. *(done 2026-05-16, PR #60 → main. **Completes Iteration 7.** Single PR (BFF + terraform + frontend, like 5.6a) — not split, not stacked. New `lambdas/api-email/` operator BFF: `GET /candidates/{id}/email` (latest EmailDraft — returns the REAL body; the operator is the authorised sender, passcode lives only inline), `PATCH .../email/{emailId}` (edit subject/body; status stays draft), `POST .../approve|reject` (status guard 409 unless draft → approved/rejected; publishes `email.approved`/`email.rejected` {businessId,draftId,websiteId,contactId}). Every edit/approve/reject writes `feedback.Capture(subject="email")`. **Passcode redaction (the iteration's core security req, 10-quality-rules §2):** `redactionCode()` KMS-decrypts `Website.passcodeCipher` (via injectable `decryptProvider`; never logs cipher/code) to recover the cleartext; `redactedJSON()` is the ONLY thing fed to `OriginalPayload`/`EditedPayload` — `strings.ReplaceAll(body/subject, code, "{{PASSCODE}}")`, and **fail-safe blanks** body+subject to "[redacted — passcode unrecoverable]" if the cipher is wiped/undecryptable so cleartext can NEVER reach the feedback row/event. `email.approved`/`email.rejected` + `feedback.captured` carry only ids/notes (no body). Leaner KMS-only `defaultDecryptProvider` (no salt/KV/Cloudflare like api-website's passcodeOps). `terraform/api-email.tf` mirrors api-website.tf — shared lambda_api role (kms:Decrypt + PutEvents already granted), 4 JWT routes, `PASSCODE_KMS_KEY_ID` env, `api_email` log group + depends_on. Frontend: `api.ts` email client (EmailDraftView/getCandidateEmail/patchEmail/approveEmail/rejectEmail); new `EmailReview.tsx` at `/queue/:businessId/email` (route added to main.tsx; "Review outreach email →" link from Candidate when website status=approved) — static-checks panel (≤200 words / opt-out present / no "password" / access-code referenced), Approve/Edit/Reject + notes, deliberately does NOT surface the passcode as a separate field (minimises cleartext surface). `/review-iteration 7.3`: skeleton-pitfall-reviewer PASS (all 20 clean; names within NAMING.md limits; no new IAM/SSM/singleton; vite base untouched). cost-idempotency-reviewer FAIL→PASS after adding the required reject-path tests (`TestHandle_Reject_RedactsPasscodeInFeedback` + `_BlanksBodyWhenCipherUnavailable`) and an editedPayload-absent assertion on the approve path; sync-BFF kill-switch/idempotency/cost-cap correctly N/A per the api-website precedent; cleartext confinement traced airtight end-to-end. go test ./... + golangci-lint clean; frontend prettier+eslint+tsc + 71 tests (11 files, +EmailReview); terraform fmt clean.)*

## Medium Priority — Iteration 8.5: Reply Triage Agent (production)

- [x] **8.5.1** `lambdas/reply-triage/` triggered by S3 PUT on the SES inbound bucket. Calls Bedrock Haiku 4.5 via `pkg/bedrock` with the `replyTriage.v1` prompt+schema (added to `07-bedrock-prompts.md` if not yet there). *(done 2026-05-16, PR #67 → main, with 8.5.2.)*
- [x] **8.5.2** Routing: `unsubscribe` (≥0.8 conf) → suppression + `Business.status='rejected_after_review'`; `positive_interest` → `Business.status='responded'`; `unknown` (<0.6 conf) → operator inbox at `/replies`. *(done 2026-05-16, PR #67 → main. 8.5.1+8.5.2 shipped together — the triage Lambda classifies AND routes, inseparable. **Spec gap**: `.ralph/specs/07-bedrock-prompts.md`/`09-iterations.md`/`02-data-model.md`/`08-admin-ui.md` do NOT enumerate replyTriage.v1 / Iteration 8.5 / a ReplyTriage item / the /replies page, and `.ralph/` is read-only — so the prompt+schema in `lambdas/pkg/` ARE the canonical definition, driven by fix_plan; documented via in-code "spec note" comments (not by editing specs). New `lambdas/pkg/bedrock` const `StageReplyTriage`; `lambdas/pkg/schemas/reply_triage_v1.go` (`ReplyTriageV1{Category enum unsubscribe|positive_interest|unknown, Confidence [0,1], Rationale ≤200}` + `ValidateReplyTriageV1` — enum/range/blank-rationale); `lambdas/pkg/prompts/reply_triage_v1.go` (Haiku 4.5, tool `classifyReply`, MaxTokens 300, EstimateUSD 0.004, CacheTTL 7d, Stage StageReplyTriage, PostValidate wired) + snapshot golden + schema round-trip/adversarial tests. New `lambdas/reply-triage/` consumes inbound replies via the project's standard **pipeline-bus → SQS** pattern (CI surfaced two hard deploy-role limits on the way: S3 rejects two notification destinations sharing a prefix+event, AND the github-actions deploy role — `aws-setup/`, do-not-touch — cannot create default-bus EventBridge rules; an S3→default-bus-EventBridge attempt was tried and reverted). Final wiring: reply-detector stays the SOLE S3 consumer (unchanged iter-8.4 single `aws_s3_bucket_notification`) and republishes a `reply.received {bucket,key}` pipeline event for EVERY parseable reply (published before its unattributed early-returns so orphan replies still reach triage); reply-triage is an `aws_cloudwatch_event_rule` on the custom pipeline bus → SQS main queue (`maxReceiveCount=3`→`dlq["reply-triage"]`) → ESM (ReportBatchItemFailures) → `pkgevents.Consume[ReplyReceivedDetail]` (mirrors sender/ses-feedback exactly). It then GetObjects the raw MIME by {bucket,key}, extracts the sender's NEW text (strips quoted original so an echoed `{{PASSCODE}}` can't leak into the excerpt/Bedrock input), attributes via the same plus-token→`REPLYREF#` lookup as reply-detector, classifies via `prompts.Invoke`, then routes — **unsubscribe & conf≥0.8** → `sesv2:PutSuppressedDestination` + `SUPPRESSION#` item (30d TTL) + `Business.status=rejected_after_review`; **positive_interest & conf≥0.6** → `Business.status=responded` (idempotent w/ reply-detector); **else / unknown / low-conf / budget-cap** → `ReplyTriage` operator-inbox item surfaced via `gsi1pk=REPLYTRIAGE#STATUS#operator_inbox` (attributed→`pk=BUSINESS#<biz> sk=REPLY_TRIAGE#<id>`, unattributed→`pk=REPLYTRIAGE#INBOX`); publishes `reply.triaged` (ids+label only, never the body). **NOT kill-switch gated** (inbound compliance — same rationale as 8.3/8.4) but the paid Bedrock call IS cost-capped: `prompts.Invoke`→`bedrock.InvokeStructured`→`cost.Assert` with `capUSD=killswitch.CapUSD(StageAudit)` (DailyBedrockUsd — fixed from an initial StageOutreach/DailyEmailUsd mistake the cost-idempotency-reviewer caught) and ledger bucket `StageReplyTriage`; `cost.ErrBudgetCapExceeded` degrades gracefully to the operator inbox + returns nil (no DLQ-loop, reply preserved in S3). `idempotency.WithIdempotency` on the S3 object key (Bedrock result also sha256(body)-cached). Status UpdateItem guarded `attribute_exists(pk) AND status<>converted`. Privacy: body sent to Bedrock (required) but never logged/evented; sender address redacted in logs (`***@domain`); excerpt has quoted-original stripped. Terraform: `reply-triage.tf` adds the SQS main queue + queue policy (events principal, ArnEquals the rule) + pipeline-bus `reply.received` rule + target→SQS + ESM + scoped `reply-triage-sqs-consume` IAM (Bedrock/SES-suppress/DDB/EB-publish already on lambda_api; `s3:GetObject` inbound/* covered by the shared-role `reply_detector_s3_read`) + log group; `reply-detector.tf` reverted to the iter-8.4 single S3→Lambda notification + `reply_detector_s3` permission; the untagged-provider alias in main.tf was reverted. No on-failure config (CI deploy role lacks PutFunctionEventInvokeConfig — iter 8.4 precedent; SQS DLQ covers loss). 11 reply-triage tests (all routing branches + budget degrade + unattributed + poison + privacy/quote-strip + idempotent replay) + prompt config/system/post-validate/snapshot + schema round-trip/adversarial. `/review-iteration 8.5.1`: bedrock-prompt-reviewer FAIL→PASS (added snapshot + schema adversarial tests; closed blank-rationale advisory); cost-idempotency-reviewer FAIL→PASS (fixed Bedrock budget bucket); skeleton-pitfall-reviewer PASS (final pipeline-SQS design clean across all 20 pitfalls — pipeline-bus rule taggable by the deploy role, scoped IAM, names ≤64). Re-reviewed after each pivot. go test ./... + golangci-lint + terraform fmt/validate clean. Live triage gated on the manual SES inbound bootstrap (docs/SES.md). 8.5.3 (/replies admin page + api-replies BFF + manual reclassify) next.)*
- [x] **8.5.3** `/replies` admin page with filter by category, manual reclassify action. *(done 2026-05-16, PR #68 → main. **Completes Iteration 8.5 (Reply Triage).** **Spec gap**: `.ralph/specs/08-admin-ui.md` does not enumerate `/replies` and `.ralph/` is read-only — the BFF + page are driven by fix_plan 8.5.3, reconciled in code comments not spec edits. New `lambdas/api-replies/` sync operator BFF (same class as api-queue/api-email — `auth.IsOperator` gate, shared lambda_api role, **no kill-switch/idempotency/cost-cap**: request/response, not an event consumer; no paid/outbound call): `GET /replies?status=&category=&limit=&cursor=` queries the existing `gsi1` (`gsi1pk=REPLYTRIAGE#STATUS#<state>`, default `operator_inbox`; also `auto_actioned`/`reviewed`), `ScanIndexForward=false` (newest-first, gsi1sk=`<createdAt>#<id>`), optional `category` FilterExpression, opaque base64 cursor (same encode/decode as api-queue); returns each item with an opaque `ref`=base64(`pk|sk`) so reclassify can address the exact row (id alone can't locate pk/sk — attributed `pk=BUSINESS#<biz> sk=REPLY_TRIAGE#<id>`, unattributed `pk=REPLYTRIAGE#INBOX`). `POST /replies/{id}/reclassify {ref,newCategory,notes}` validates newCategory∈{unsubscribe,positive_interest,unknown}, decodes+cross-checks ref vs path id (bad inputs→400 not 500), UpdateItem the ReplyTriage row (category, triageState=`reviewed`, gsi1pk=`REPLYTRIAGE#STATUS#reviewed` so it leaves the inbox, operatorAction/operatorNotes, updatedAt, guarded `attribute_exists(pk)`), then for attributed replies re-applies the classifier's Business side-effect (`unsubscribe`→status `rejected_after_review`, `positive_interest`→`responded`) via the shared guarded `setStatus` (`attribute_exists(pk) AND status<>converted`). **Scope note** (spec silent, documented in code): reclassify does NOT re-suppress in SES because the prospect's address is deliberately not stored on the item; the auto path already suppressed at conf≥0.8. Terraform `api-replies.tf` mirrors api-queue.tf exactly (archive, lambda, apigw permission/integration AWS_PROXY 2.0, two JWT+cognito-authorizer routes) + `api_replies` log group; no new IAM (DDB rw already on lambda_api), no async/DLQ (sync BFF). Frontend: `getReplies`/`reclassifyReply` + `ReplyItem`/`RepliesResponse` in `api.ts`; new `Replies.tsx` (inbox/category selectors + Refresh, per-card created/category+confidence/rationale/excerpt, inline reclassify select+notes+Apply that drops the item on success); `/replies` route in `main.tsx` + nav link in `App.tsx`; `Replies.test.tsx` (render+badge+excerpt, reclassify→POST body {ref,newCategory}+item drops, error banner, empty state). `/review-iteration 8.5.3`: cost-idempotency-reviewer PASS (sync-BFF exemption correct; auth gate; guarded conditional writes; input validation 400s; cursor opaque/safe; no privacy/cost regression); skeleton-pitfall-reviewer PASS (bootstrap/log-group+depends_on/no-new-IAM/name-length/apigw-wiring all clean; no async pitfall). go test ./... + golangci-lint + terraform validate clean; frontend 75 tests + lint + typecheck + format:check clean. Iteration 9 (Feedback Capture & Learning Loop) next.)*

## Medium Priority — Iteration 8: SES Sending + Tracking + Passcode Cleanup

- [x] **8.1** SES configuration set wired in iteration 0.B; verify domain verification status check. *(done 2026-05-16, PR #61 → main. The SES config-set/identity/DKIM/MAIL-FROM/SNS infra already exists from 0.C.4 (`terraform/ses.tf`; the fix_plan's "iteration 0.B" is a mislabel — it's 0.C.4). 8.1 adds the missing **domain-verification status check**: new operator-only `GET /email/status` route on the existing api-email Lambda → `handleEmailStatus` calls `sesv2:GetEmailIdentity` for the project-global `outreach.<base_domain>` singleton and returns `{identity, verifiedForSending, dkimStatus, dkimSigningEnabled, mailFromDomain, mailFromDomainStatus}`. Prod-only identity → in per-PR envs it **degrades gracefully** (logs at info, returns 200 with `verifiedForSending:false`/`dkimStatus:UNKNOWN`) rather than 5xx; empty `SES_OUTREACH_IDENTITY` → 500; operator-gated (403) via the existing `handle()` gate; `sesStatusProvider` injectable for tests. `terraform/api-email.tf`: the JWT route, `SES_OUTREACH_IDENTITY="outreach.${var.base_domain}"` env, and a new least-privilege inline policy `api-email-ses-status` granting only `sesv2:GetEmailIdentity` on the specific identity ARN (same ARN shape as ses.tf's `ses_send`; additive on the shared lambda_api role). Added `service/sesv2` to go.mod. New **`docs/SES.md`** (promised by 0.C.4, never written): the Terraform-managed DKIM ×3 / MAIL-FROM MX+SPF records, the recommended manual DMARC record, how to check status (UI + CLI), and the one-time sandbox-out procedure (hard prerequisite for 8.2 live sends). Frontend: `api.ts getEmailStatus()` + a compact read-only "SES email infrastructure" panel on the Settings page (best-effort; load failure shows an info line, never blocks the settings form). `/review-iteration 8.1`: skeleton-pitfall-reviewer PASS (all 20 clean — IAM least-privilege/no-wildcard/shared-role, no new secret/SSM/singleton, vite base untouched), cost-idempotency-reviewer PASS (sync-BFF kill-switch/idempotency/cost-cap correctly N/A; GetEmailIdentity is a free control-plane read; route ordering + graceful degradation + injectability + 4 test branches verified). go test ./... + golangci-lint clean; frontend prettier+eslint+tsc + 71 tests; terraform fmt clean. 8.2 (sender Lambda) is next and depends on the manual sandbox-out in docs/SES.md.)*
- [x] **8.2** `lambdas/sender/` with `MessageDeduplicationId = sha256(contactId+websiteId)`; suppression check before send. *(done 2026-05-16, PR #62 → main. New `lambdas/sender/` SQS consumer of `email.approved` (api-email is the producer, iter 7.3). `killswitch.WithKillSwitch(StageOutreach)` → `idempotency.WithIdempotency(eventId)` (replay safety for the same approval). Loads EmailDraft+Contact; defensive guard `draft.Status=="approved"`; skip on missing contact email. **Send-once guard** = the fix_plan's "MessageDeduplicationId": a PERMANENT (no-TTL) conditional-PutItem marker `pk=BUSINESS#<id> sk=EMAIL_SENT#<sha256(contactId|websiteId)>` claimed BEFORE the SES call, so two *distinct* approvals for the same (contact,website) — which idempotency-on-eventId would NOT catch — never both send, and a post-send write-failure + retry can't double-send. **Suppression fails CLOSED**: `sesv2:GetSuppressedDestination` → err==nil ⇒ suppressed (skip), NotFoundException ⇒ proceed, ANY other error ⇒ return error (DLQ, never send). SES send is raw-MIME (`sesv2.SendEmail` Content.Raw) from the pinned `outreach@outreach.<base_domain>` + config set, with the RFC-8058 one-click `List-Unsubscribe: <https…/unsubscribe?d=>, <mailto…>` + `List-Unsubscribe-Post: List-Unsubscribe=One-Click` headers (mailto works now; the https endpoint is a later sub-item); subject CRLF-sanitised (header-injection guard). SES send wrapped in `cost.WithCostCap(StageOutreach, $0.0001, CapUSD→DailyEmailUsd)` per the project's "every paid call wraps" rule. On success: write `EmailEvent` event=sent (`sk=EMAIL_EVENT#<ts>#<sesMsgId>`), flip `EmailDraft.status=sent`, publish `email.sent {businessId,draftId,websiteId,contactId,sesMessageId,sentAt}` — NO body/address; completion + suppression-skip logs omit subject/body/address. Terraform `sender.tf` mirrors email-draft.tf (EB `email.approved`→SQS visibility 120>timeout 60, retry+DLQ `dlq["sender"]`, ArnEquals queue policy, ESM ReportBatchItemFailures, scoped SQS-consume inline IAM); env `SES_FROM_ADDRESS`/`SES_CONFIGURATION_SET`/`UNSUBSCRIBE_BASE=https://${local.api_domain}` (env-aware, not hardcoded prod); no new SES IAM (ses.tf `ses_send` already grants SendEmail + GetSuppressedDestination); `sender` log group. 9 tests incl. happy (raw-MIME headers + EmailEvent + status=sent + email.sent no-leak sweep), suppressed→no-send, suppression-error→fail-closed, already-sent→no-second-send, not-approved/no-email skip, dedup-key stable/distinct, CRLF-strip, killswitch-off. `/review-iteration 8.2`: cost-idempotency-reviewer FAIL→PASS after wrapping the SES send in cost.WithCostCap (kill-switch/idempotency/permanent-marker-ordering/suppression-fail-closed/no-PII all clean); skeleton-pitfall-reviewer FLAGGED→PASS after switching `UNSUBSCRIBE_BASE` to env-aware `local.api_domain` (all other pitfalls clean: bootstrap/log-group/scoped-IAM/no-new-secret/names within NAMING.md). go test ./... + golangci-lint clean; terraform fmt clean. Live delivery still gated on the manual SES sandbox-out (docs/SES.md). 8.3 (bounce/complaint SNS→suppression) next.)*
- [x] **8.3** Bounce/complaint webhook receiver via SNS → Lambda; updates suppression. *(done 2026-05-16, PR #63 → main. New `lambdas/ses-feedback/` SQS consumer of the SES configuration-set feedback notifications. Pipeline: SES config-set event destination `feedback_sns` (0.C, BOUNCE/COMPLAINT/DELIVERY/REJECT/RENDERING_FAILURE) → SNS `ses_feedback` (ses.tf) → new SQS `ses-feedback-main` (SNS subscription, `raw_message_delivery=false`) → Lambda ESM ReportBatchItemFailures → 3 retries → `dlq["ses-feedback"]`. Manual SNS-envelope parse (`{Type,Message}`) then SES JSON — cannot use `pkgevents.Consume` (SNS envelope ≠ project Envelope), so manual `BatchItemFailures`. **Deliberately NOT kill-switch gated**: bounce/complaint handling is inbound compliance work that must run even when outreach is paused (you pause *because* reputation is degrading); a `WithKillSwitch(StageOutreach)` wrapper would, when off, return success and silently ack/discard the whole SQS batch, losing the project Suppression item + audit trail — skeleton-pitfall-reviewer caught this as a correctness flag and the wrapper was removed. `idempotency.WithIdempotency(sesMessageId+":"+kind)` (SNS redelivery + SES duplicate safety). `kind()` = eventType, falling back to notificationType. Routing: **permanent bounce** → `sesv2:PutSuppressedDestination` + project `SUPPRESSION#<lc-email>` item (30d `expires_at` TTL, reason=bounce) + EmailEvent(bounced) + publish `email.bounced`; **transient bounce** → NO suppression, EmailEvent(bounced) + `email.bounced` only; **complaint** → always suppress (reason=complaint) + EmailEvent(complained) + `email.complained`; **delivery** → EmailEvent(delivered) + `email.delivered`, no suppression; unknown → log no-op. Business attribution via the `SESMSG#<msgId>` reverse index written by the sender (additive change to `lambdas/sender/`: `SESMsgIndexRow`/`PutMsgIndex`/`putMsgIndex` + buildDeps wiring + test — a bounce SNS notification only carries sesMessageId+recipient, so the index is how we map back to business/draft). **Fail-safe**: missing reverse index ⇒ suppression still happens (compliance is email-keyed), only the EmailEvent is skipped; `email.bounced` still publishes with empty businessId. No paid/outbound-HTTP call (sesv2 suppression + DDB + EventBridge are free control-plane) ⇒ no cost cap, correctly. No address/passcode/PII in any log line or published event detail except the recipient address in `FeedbackDetail` (structured EventBridge detail over the encrypted bus, needed downstream — not a log). Terraform: `ses-feedback.tf` (Lambda+SQS+SNS subscription+queue policy ArnEquals `ses_feedback` topic+ESM+scoped SQS-consume IAM; no new SES IAM — ses.tf `ses_send` already grants PutSuppressedDestination), `ses_feedback` log group, `alarms.tf` (production-only `ops_alerts` SNS topic + account-level `AWS/SES Reputation.BounceRate ≥5%` / `Reputation.ComplaintRate ≥0.1%` 24h alarms — singletons gated on `is_production` like the SES domain identity; operator subscribes the topic manually). 9 tests: permanent-bounce→suppress+Suppression+EmailEvent+email.bounced, transient→no-suppress, complaint→suppress, delivery→event-only, missing-index→suppress-but-no-EmailEvent+no-businessId, idempotent replay no-op, malformed→dropped(nil), handle drops poison + not kill-switch gated. `/review-iteration 8.3`: cost-idempotency-reviewer PASS (all five checks; no paid call so cost cap correctly absent; partial-batch + fail-safe correct; no PII leak); skeleton-pitfall-reviewer PASS after removing the kill-switch wrapper (bootstrap/log-group+depends_on/scoped-IAM/name-length all clean; per-Lambda SQS-consume inline policy and production-only account-metric alarms are established repo conventions, documented). go test ./... + golangci-lint + terraform fmt/validate clean. 8.4 (reply detection via SES inbound rule + S3) next.)*
- [x] **8.4** Reply detection via SES inbound rule + S3 + Lambda → marks `Business.status='responded'`. *(done 2026-05-16, PR #64 → main. New `lambdas/reply-detector/` S3-triggered Lambda. **Deterministic attribution** (no fragile In-Reply-To/Message-ID matching): additive sender change adds a plus-addressed `Reply-To: outreach+<draftId>@outreach.<base_domain>` header (`plusAddress` helper strips CR/LF/@/+ from the token — header-injection safe) + writes a `REPLYREF#<draftId>` reverse index (reuses the SESMsgIndexRow shape/PutMsgIndex dep; keyed by draftId which is known pre-send, unlike the post-send sesMessageId). Pipeline: SES inbound receipt rule (recipients=`outreach.<base_domain>`, scan_enabled, TLS Require, S3 action prefix `inbound/`) → private SSE S3 bucket → `s3:ObjectCreated:*` notification (filter_prefix `inbound/`) → Lambda. `reply-detector` parses the raw MIME with `net/mail`, scans To/Cc/Delivered-To/X-Original-To/X-Forwarded-To for the first plus-addressed recipient at the reply domain, takes the +token as draftId, looks up `REPLYREF#<draftId>` → flips Business.status→`responded` (UpdateItem SET status+gsi1pk+respondedAt+updatedAt, ConditionExpression `attribute_exists(pk) AND status<>converted` so a converted business is never regressed; CCFE⇒no-op), writes `EmailEvent(event=replied)`, publishes `email.replied` {businessId,draftId,websiteId,contactId,repliedAt}. **NOT kill-switch gated** (inbound relationship work must run even when outreach paused — same rationale as 8.3). `idempotency.WithIdempotency(consumer="reply-detector", key=S3 object key)` — S3-redelivery + async-retry safe; unattributable/unparseable replies return nil (dropped, not DLQ-looped — the raw object persists 90d in S3 for manual/8.5 inspection). Hard errors return → S3 async invoke retries (×2 default); the raw object also persists in the inbound bucket for the 90-day lifecycle so a reply is never silently lost (no explicit on-failure DLQ — that would need `lambda:PutFunctionEventInvokeConfig` on the do-not-touch aws-setup CI deploy role; out of 8.4 scope). **Privacy** (non-negotiable for this iter): the reply body, subject, and prospect address are NEVER logged or placed in the EmailEvent row or `email.replied` detail — only journey ids; a test asserts a body marker string is absent from the marshalled event+payload. No paid/outbound-HTTP call (S3 Get + DDB + EventBridge are free) ⇒ no cost cap, correctly. Terraform `reply-detector.tf`: per-env private inbound bucket (SSE AES256, public-access-block, 90d expiry lifecycle, `force_destroy=!is_production`), Lambda (env REPLY_DOMAIN=`outreach.<base_domain>`), S3→Lambda permission+notification, scoped `s3:GetObject`(inbound/*) IAM; SES receipt rule set / active rule set / receipt rule + receiving MX are production-only `count` singletons (mirrors ses.tf's domain-identity pattern, pitfall #10 spirit); bucket policy grants `ses.amazonaws.com s3:PutObject` scoped via SourceAccount+SourceArn(rule-set). `reply_detector` log group. 8 reply-detector tests (happy + body-leak sweep, attribution via Delivered-To, no-+token unattributed, wrong-domain ignored, missing ref unattributed, unparseable dropped, idempotent replay, plusToken unit) + 2 new sender tests (Reply-To header + REPLYREF write, plusAddress sanitisation). `/review-iteration 8.4`: cost-idempotency-reviewer PASS (idempotency/no-cost/privacy all clean; unattributable-drop design sound); skeleton-pitfall-reviewer PASS / no regressions (#1 force_destroy, #3 bootstrap, #5 log-group+depends_on, #6 scoped IAM, #8 name lengths within limits, #10 production-only singletons all clean). go test ./... + golangci-lint + terraform fmt/validate clean. Live inbound still gated on the manual SES domain-receive + sandbox-out + active-rule-set bootstrap (docs/SES.md). 8.5 (passcode cleanup 24h post email.sent) next.)*
- [x] **8.5** Passcode cleanup Lambda: 24h after `email.sent`, zap `Website.passcodeCipher`; emit `preview.passcode.cleartext_wiped`. *(done 2026-05-16, PR #65 → main. Additive sender change: after a successful send the sender stamps `Website.passcodeCleanupDueAt = d.Now()+24h` (unix) on the Website item (`pk=BUSINESS#<biz> sk=WEBSITE#<webId>`) via a new best-effort `SetCleanupDue` dep/`setCleanupDue` helper (ConditionExpression `attribute_exists(pk)`, CCFE→no-op) — a stamp failure only `logger.Warn`s, never fails the send (the sweep is the authority + 8.6 adds the `passcodeRevealableUntil` backstop). New scheduled `lambdas/passcode-cleanup/` Lambda: hourly EventBridge Scheduler (`rate(1 hour)`, ENABLED every env, scheduler_invoke role, retry 3) → `sweep`: paginated `Scan` `#t=:w AND attribute_exists(passcodeCipher) AND passcodeCleanupDueAt<=:now` **projecting pk,sk ONLY** (cleartext never pulled into memory), then per item a conditional `UpdateItem REMOVE passcodeCipher, passcodeCleanupDueAt SET updatedAt` guarded on `attribute_exists(passcodeCipher)` (blind remove — value never read), then publishes `preview.passcode.cleartext_wiped {businessId,websiteId,reason=sent_24h,wipedAt}`. Only `passcodeCipher` (+ the due marker) is touched — `passcodeHash` and the Cloudflare KV mapping stay intact so the recipient link keeps working; only the operator's re-view ability goes away (regenerate to resend). **NOT kill-switch gated** (privacy/retention guarantee runs regardless of outreach state — same rationale as 8.3/8.4). **Idempotent without a wrapper**: the conditional update is the atomic gate — re-runs and concurrent/overlapping runs that lose the race get CCFE→`did=false`→skip publish (no double-publish; reviewer-verified). One failed item logs+continues but the sweep returns an aggregate error so the schedule retries. No paid/outbound call (DDB Scan/UpdateItem + EventBridge only) ⇒ no cost cap, correctly. 6 passcode-cleanup tests (wipe+publish+privacy-key sweep on the event JSON, already-wiped→no-publish, one-fail→continue+aggregate-error, scan-error→abort, scanDue pagination+pk/sk-only-projection+filter assertions) + extended sender happy-path (cleanup-due = now+24h unix, businessId). `/review-iteration 8.5`: cost-idempotency-reviewer PASS (idempotency reasoning sound, no double-publish, privacy invariant confirmed — cleartext never in memory/logs/events, KV/hash untouched, no cost cap needed, sweep error handling correct); skeleton-pitfall-reviewer PASS / no regressions (#3 bootstrap, #5 log-group+depends_on, #6 no new IAM — shared lambda_api already has DDB Scan/UpdateItem+EventBridge and scheduler_invoke wildcard covers the new fn, #8 name 59≤64, scheduler wiring matches cost-rollover/discover pattern). go test ./... + golangci-lint + terraform fmt/validate clean. 8.6 (daily passcodeRevealableUntil TTL sweep — extends this same sweep) next.)*
- [x] **8.6** Passcode TTL sweep daily: zap `passcodeCipher` past `passcodeRevealableUntil`. *(done 2026-05-16, PR #66 → main. Logic-only extension of the iter-8.5 `lambdas/passcode-cleanup/` hourly sweep — no Terraform/IAM/schedule change (the existing hourly EventBridge Scheduler + shared lambda_api role already cover it; **hourly subsumes the spec's "daily"** — one Lambda + one schedule beats a separate daily job). Scan FilterExpression broadened to `#t=:w AND attribute_exists(passcodeCipher) AND (passcodeCleanupDueAt <= :now OR passcodeRevealableUntil <= :now)` so a Website is wiped when EITHER the 24h-post-send marker (8.5) OR the publisher-set `passcodeRevealableUntil` backstop has passed **even if no email was ever sent** (forces regenerate-and-resend if the operator sat on the candidate). ProjectionExpression now also returns the two NON-secret timestamp markers (`passcodeCleanupDueAt`,`passcodeRevealableUntil`) — `passcodeCipher` is STILL never projected/read; a `wipeReason` helper attributes each wipe `sent_24h` (cleanupDue present & ≤now) vs `revealable_expired` (otherwise — the OR guarantees at least one matched), threaded through `websiteRef.Reason` into the `preview.passcode.cleartext_wiped` event. Wipe path unchanged (blind conditional `REMOVE passcodeCipher, passcodeCleanupDueAt` guarded on `attribute_exists(passcodeCipher)`); DynamoDB treats a missing `passcodeCleanupDueAt` in the comparison as false (not an error) so never-emailed Websites match only the backstop branch. Tests updated: 2-reason sweep (sent_24h + revealable_expired pass-through), scanDue pagination now asserts the new projection (keys+timestamps, **never passcodeCipher**) + OR-filter + per-item reason attribution (emailed→sent_24h, never-emailed→revealable_expired). `/review-iteration 8.6`: cost-idempotency-reviewer PASS (idempotency atomic-gate unchanged, no double-publish, **privacy intact — cipher never projected/read/logged/emitted, hash+KV untouched**, OR-filter absent-attr semantics correct, no cost cap needed); skeleton-pitfall-reviewer PASS (no Terraform correctly — reuses existing schedule+role+attributes; all 20 pitfalls N/A or clean). go test ./... + golangci-lint clean. **Completes Iteration 8 (SES Sending + Tracking + Passcode Cleanup).** Iteration 8.5 Reply Triage (8.5.1–8.5.3) next.)*

## High Priority — Iteration 9: Feedback Capture & Learning Loop

- [x] **9.1** `feedback.captured` events from every Approve/Edit/Reject (carries from iters 4/5/6/7); passcode-redacted in body. *(verified 2026-05-16, PR #69 → main — carry-over checkpoint, no code change needed. `pkg/feedback.Capture` (writes the `FEEDBACK#<vertical>` row + publishes `feedback.captured`) is already called on every operator decision: `lambdas/api-specs` (spec edit→ActionEdit, approve→ActionApprove, reject→ActionReject via handleDecision), `lambdas/api-website` (approve/reject via handleDecision, regenerate-passcode + regenerate-site→ActionRegenerate), `lambdas/api-email` (edit→ActionEdit, approve/reject via handleDecision — **both Original+EditedPayload run through `redactedJSON` which substitutes the KMS-decrypted cleartext with `{{PASSCODE}}`, or blanks the body if KMS is unavailable**, so the cleartext never lands in a Feedback row). Audit/qualification have no operator approve/edit/reject surface (pipeline-internal) so the spec's "Audit" line is satisfied vacuously. Prior `/review-iteration` runs (6.3/7.3) already validated the redaction paths; re-confirmed here by the 9.2 cost-idempotency-reviewer which checked the new log BFF never re-exposes those payloads.)*
- [x] **9.2** `/feedback` log UI. *(done 2026-05-16, PR #69 → main. **Spec gap**: `.ralph/specs/08-admin-ui.md` doesn't enumerate `/feedback` and `.ralph/` is read-only — driven by fix_plan 9.2, reconciled in code comments. New `lambdas/api-feedback/` sync operator BFF (same exempt class as api-queue/api-replies — `auth.IsOperator` gate, shared lambda_api role, no kill-switch/idempotency/cost-cap, read-only DDB): `GET /feedback?vertical=&subject=&limit=&cursor=` Queries the base table `pk=FEEDBACK#<vertical>` (default `default`), `sk=<createdAt>#<id>` so `ScanIndexForward=false` is a clean reverse-chron log per vertical (the base table beats gsi2 whose sk is subject-prefixed), optional `subject` FilterExpression (whitelisted to pkg/feedback's closed Subject set), opaque base64 pk|sk cursor (same encode/decode as api-queue). **Privacy**: `FeedbackRow`/`feedbackItem` deliberately OMIT `originalPayload`/`editedPayload` — `attributevalue.UnmarshalMap` discards the unmapped attrs so the (email-redacted) draft bodies are never read into memory or returned by the log; a test asserts the response carries neither `PASSCODE` nor `originalPayload`. Terraform `api-feedback.tf` mirrors api-queue.tf exactly (archive/lambda/apigw permission+integration AWS_PROXY 2.0/`GET /feedback` JWT+cognito route) + `api_feedback` log group; no new IAM, no async/DLQ. Frontend: `getFeedback`+`FeedbackItem`/`FeedbackResponse` in `api.ts`; new `Feedback.tsx` (vertical input + stage select [server-side] + since-date [client-side over the page] filters, reverse-chron table of when/stage/action/subject/actor/notes — no payload bodies); `/feedback` route in `main.tsx` + nav link in `App.tsx`; `Feedback.test.tsx` (rows render, stage filter → `subject=` query, error banner). 6 api-feedback tests (auth 403, non-GET 405, default-vertical newest-first + **no-payload-leak assertion**, vertical+subject filter, bad subject/limit 400, cursor round-trip). `/review-iteration 9.2`: cost-idempotency-reviewer PASS (sync-BFF exemption; auth gate; no paid call; input validation 400s; **privacy projection structurally enforced + test-covered**); skeleton-pitfall-reviewer PASS (all 20 pitfalls clean; mirrors api-queue). go test ./... + golangci-lint + terraform validate clean; frontend 78 tests + lint + typecheck + format:check clean. 9.3 (weekly tuner Lambdas) next.)*
- [x] **9.3** Weekly tuner Lambdas: `tuner-targeting`, `tuner-style`, `tuner-email-tone` (EventBridge Scheduler Sunday 02:00 UTC). *(done 2026-05-16, PR #70 → main. Three `tuner.*.v1` prompts (`lambdas/pkg/prompts/tuner_{style,email_tone,targeting}_v1.go`) with **hand-written `*SchemaRaw` verbatim from `.ralph/specs/07-bedrock-prompts.md`** (style=Sonnet/proposeStyleDelta, email-tone+targeting=Haiku) — style System is the spec block verbatim; email-tone/targeting have no spec System so prose is authored + flagged as a spec-gap in code (07 only gives those two a Tool). `lambdas/pkg/schemas/tuner_{...}_v1.go` structs + validators: rationale-non-empty; style+email-tone reject invented business facts (testimonial/award/price/star/%/£$€) via `inventedFactPatterns` ∪ new `tunerBannedFactPatterns`/`matchesInventedFact`, scanning **all additive lists incl. addProhibitedPhrases**; targeting clamps weightDeltas to ±0.2. **Per 07-bedrock-prompts.md L378 tuner prompts are NOT cached** — `CacheTTL:0` + `PromptProposer` passes an empty cacheKey so `bedrock.InvokeStructured` bypasses the cache (every weekly run sees fresh feedback); `cost.Assert` still runs. Shared `lambdas/pkg/tunerlib`: `Run` loops verticals (env `TUNER_VERTICALS`=default,accountants), aggregates last 7d Feedback per vertical (base-table `pk=FEEDBACK#<vertical>`, `sk>since`, subject-filtered: style←spec/website, email-tone←email, targeting←audit/qualification), loads the current profile (STYLE#/EMAIL_TONE#<vertical>; targeting has no per-vertical item → null, documented MVP), calls Bedrock via injected `Propose` (prod = generic `PromptProposer` running `prompts.Invoke` with `capUSD=killswitch.CapUSD(StageAudit)`→DailyBedrockUsd, ledger bucket = the tuner's bedrock.Stage), writes a PENDING `TunerDelta` + publishes `tuner.delta.proposed`. **NOT kill-switch gated** (StageMap→""; weekly schedule). `cost.ErrBudgetCapExceeded`→`Run` returns nil (skip the week, no DLQ-loop); non-cap errors propagate (schedule retries ×3). **Idempotency**: `TunerDelta` SK = stable `weekID` (sha of ISO-week+kind+vertical, NO timestamp) so a same-week retry overwrites instead of duplicating; `gsi1pk=DELTA#STATUS#pending` (+ `gsi1sk=<ts>#id` for newest-first /tuners ordering — one GSI entry per base item, overwrite-safe). 3 thin Lambda mains delegate to tunerlib. Terraform `tuners.tf` (for_each over the 3: archive/lambda shared lambda_api role/log group+depends_on; no new IAM — DDB+EB+Bedrock already granted, scheduler_invoke wildcard covers the fns) + flipped the 3 pre-existing `aws_scheduler_schedule` rules (eventbridge.tf, cron Sun 02:00/02:05/02:10 UTC) DISABLED→ENABLED. Tests: 3 prompt config/system/postvalidate/snapshot + 3 schema roundtrip/adversarial (incl. addProhibitedPhrases + negative-OOB weight + addAntiPatterns invented-fact) + tunerlib Run (per-vertical propose, skip-no-feedback, budget-cap→nil, propose-error propagates, weekID stable/varies). `/review-iteration 9.3`: bedrock-prompt-reviewer FAIL→PASS (fixed no-cache, email-tone addProhibitedPhrases scan, added missing adversarial cases); cost-idempotency-reviewer FAIL→PASS (fixed SK→stable weekID, overwrite-safe); skeleton-pitfall-reviewer PASS (all 20 pitfalls clean — bootstrap/log-group+depends_on/no-new-IAM/names ≤64/scheduler-via-scheduler_invoke proven pattern). go test ./... + golangci-lint + terraform validate clean. 9.4 (`*Delta` apply/dismiss + /tuners UI) next.)*
- [x] **9.4** `*Delta` items + `/tuners` UI (apply / dismiss / diff). Operator-only; no auto-apply at MVP. *(done 2026-05-16, PR #71 → main. **Completes Iteration 9 (Feedback Capture & Learning Loop).** The `TunerDelta` item already exists (iter 9.3, `pkg/tunerlib`); 9.4 adds the operator review surface. **pkg/feedback contract extension** (additive + minimal): new `SubjectProfile="profile"`, `ActionApply`/`ActionDismiss`; `validate()` allows empty `businessId` ONLY when `Subject==SubjectProfile` (profile tuning is vertical-scoped, not business-scoped — `SubjectID`=deltaId; every other subject still requires businessId — no regression); stale enum error strings updated; new positive test. New `lambdas/api-tuners/` sync operator BFF (api-replies class — `auth.IsOperator`, shared role, no kill-switch/idempotency/cost-cap, no paid call): `GET /tuners?status=` lists deltas via `gsi1pk=DELTA#STATUS#<status>` (default pending, newest-first) returning `ref`=base64(pk|sk) + parsed `proposed` payload for the diff; `POST /tuners/{id}/apply {ref}` and `/dismiss {ref,reason}`. **Apply** parses `proposedPayload` by kind and mutates the live profile: style→`style.Get`+`mergeList`(add-then-remove, dedupe, trim) on Do/Dont/AntiPatterns (+ palette.Primary if suggested)→`style.Update` (atomically bumps `version`); email_tone→`tone.Get`+merge subject/opener/prohibited→`tone.Update` (bumps version); **targeting=advisory MVP** (no canonical per-vertical TargetingProfile — recorded+audited+evented, operator wires nudges via /settings/targeting; documented spec-gap). Then conditional `UpdateItem` flips the delta status (`ConditionExpression attribute_exists(pk) AND status=pending` so a double-apply/apply-after-dismiss race can't double-mutate — loser gets **409**, not a misleading 500), `feedback.Capture(Subject=profile, Action=apply/dismiss, Vertical, SubjectID=deltaId)` writes the audit row (lands in `FEEDBACK#<vertical>` → visible at /feedback), and apply publishes `profile.updated {kind,vertical,version,deltaId}`. Dismiss: status→dismissed + Feedback row, no profile change/event. The profile mutation is itself etag-optimistic-locked by style/tone.Update (second concurrent apply → ErrEtagMismatch → 500, profile never double-mutated). Terraform `api-tuners.tf` mirrors api-replies.tf (3 JWT routes) + log group; no new IAM. Frontend: `getTuners/applyTuner/dismissTuner`+types in `api.ts`; new `Tuners.tsx` (pending list: kind/vertical/rationale + proposed payload pretty-printed as the diff, Apply/Dismiss, drops on success); `/tuners` route+nav; `Tuners.test.tsx`. 10 api-tuners tests (auth, list-default-pending, bad-status 400, targeting-advisory apply records+2 events, dismiss 1 event no profile.updated, not-pending 409, bad-input 400s, mergeList, ref roundtrip) + feedback profile-subject test + 4 frontend tests. `/review-iteration 9.4`: cost-idempotency-reviewer FAIL→PASS (fixed CCFE-race→409, stale strings, added feedback test); skeleton-pitfall-reviewer PASS (mirrors api-replies; bootstrap/log-group+depends_on/no-new-IAM/name-length all clean). go test ./... + golangci-lint + terraform validate clean; frontend lint+typecheck+format+suite clean. Iteration 10 (Step Functions Orchestration) next.)*

## Low Priority — Iteration 10: Step Functions Orchestration

- [ ] **10.1** `WebsiteGenerationStateMachine` (Express).
- [ ] **10.2** `OutreachStateMachine` (Standard).
- [ ] **10.3** Migrate iter 4–8 producers to invoke state machines.

## Low Priority — Iteration 11: Analytics & Funnel

- [ ] **11.1** Funnel metrics roll-up Lambda.
- [ ] **11.2** Cost dashboard.
- [ ] **11.3** Per-vertical comparison + profile-version splits.

## Completed
- [x] Specifications written (`.ralph/specs/`)
- [x] Repo `reaandrew/ai-website-agency-spec` (spec) created and pushed
- [x] Repo `reaandrew/cloud-skeleton` made a GitHub template

## Notes
- Iteration 0 is split into A/B/C/D/E/F because it's a substantial bring-up. They can be parallelized within reason but A.1–A.6 must complete before B/C/D/E/F start.
- Every iteration must end with a working demo. The skeleton's per-PR ephemeral env is the demo surface.
- Read `10-quality-rules.md` before generating any AI content. Violating those rules is a hard fail.
- Cost discipline: see `05-capacity-and-cost.md`. Total target spend at moderate caps is £60–£100/month at MVP load.
- Passcode cleartext is a secret. Never log, never event, never DDB-unencrypted. See iteration 5 + 7 + 8 specifics.
- Skeleton pitfall mitigations are non-negotiable. See `01-architecture.md` § Skeleton-honored constraints.
